# 开发运行调试 v32：失败事件管理页面动作权限预检

## 本次目标

v32 控制成一个清晰小闭环：

```text
使用 v31 已返回的 allowedActions / deniedActions
在页面身份校验后禁用当前角色未授权的写按钮
```

不扩大后端模型，不新增数据库结构，只把现有动作权限快照落到页面交互里。

## 代码改动

页面脚本：

```text
src/main/resources/static/failed-events.js
```

新增受控按钮缓存：

```javascript
"markButton",
"requestReplayApprovalButton",
"approveReplayButton",
"rejectReplayButton",
"replayButton",
```

校验身份成功后应用权限：

```javascript
const ability = operatorAbilitySummary(result);
statusElement.textContent = `${summary} | ${ability.short}`;
statusElement.title = ability.long;
applyOperatorPermissions(scope, result.allowedActions || []);
```

校验失败时禁用当前区域写按钮：

```javascript
statusElement.textContent = "校验失败";
statusElement.removeAttribute("title");
applyOperatorPermissions(scope, []);
```

动作到按钮的映射：

```javascript
function actionControls(scope) {
    if (scope === "management") {
        return [
            {
                action: "MANAGE_FAILED_EVENT",
                buttons: [elements.markButton]
            }
        ];
    }
    return [
        {
            action: "REQUEST_REPLAY_APPROVAL",
            buttons: [elements.requestReplayApprovalButton]
        },
        {
            action: "REVIEW_REPLAY_APPROVAL",
            buttons: [elements.approveReplayButton, elements.rejectReplayButton]
        },
        {
            action: "REPLAY_FAILED_EVENT",
            buttons: [elements.replayButton]
        }
    ];
}
```

按钮禁用逻辑：

```javascript
function applyOperatorPermissions(scope, allowedActions) {
    const allowedActionSet = new Set(allowedActions);
    actionControls(scope).forEach(({ action, buttons }) => {
        const enabled = allowedActionSet.has(action);
        buttons.forEach((button) => {
            button.disabled = !enabled;
            button.classList.toggle("permission-denied", !enabled);
            if (enabled) {
                button.removeAttribute("title");
            } else {
                button.title = `${actionLabel(action)} 未授权，请切换角色后重新校验身份`;
            }
        });
    });
}
```

切换操作人或角色后恢复未校验：

```javascript
elements.operatorIdInput.addEventListener("input", () => resetOperatorPermissions("management"));
elements.operatorRoleInput.addEventListener("change", () => resetOperatorPermissions("management"));
elements.replayOperatorIdInput.addEventListener("input", () => resetOperatorPermissions("replay"));
elements.replayOperatorRoleInput.addEventListener("change", () => resetOperatorPermissions("replay"));
```

样式：

```text
src/main/resources/static/failed-events.css
```

禁用态扩展：

```css
.primary-button:disabled,
.secondary-button:disabled,
.ghost-button:disabled {
    cursor: not-allowed;
    opacity: 0.55;
}
```

未授权按钮提示样式：

```css
.permission-denied {
    border-style: dashed;
    filter: grayscale(0.4);
}
```

页面静态测试：

```text
src/test/java/com/codexdemo/orderplatform/FailedEventManagementPageTests.java
```

新增断言：

```java
"applyOperatorPermissions",
"resetOperatorPermissions",
"actionControls",
"permission-denied",
".permission-denied"
```

## 文档改动

```text
README.md
代码讲解记录/36-version-32-page-action-permission-precheck.md
代码讲解记录/README.md
a/32/解释/说明.md
```

## 已执行验证

基础检查：

```powershell
mvn -DskipTests compile
node --check src/main/resources/static/failed-events.js
```

结果：

```text
compile -> BUILD SUCCESS
node --check -> OK
```

相关测试：

```powershell
mvn "-Dtest=FailedEventManagementPageTests,FailedEventOperatorContextIntegrationTests" test
```

结果：

```text
Tests run: 3, Failures: 0, Errors: 0, Skipped: 0
```

完整测试：

```powershell
mvn test
```

结果：

```text
Tests run: 33, Failures: 0, Errors: 0, Skipped: 0
```

打包：

```powershell
mvn -DskipTests package
```

结果：

```text
BUILD SUCCESS
```

HTTP smoke：

```text
/actuator/health -> UP
/failed-events.html -> 200
/operator-context + ORDER_SUPPORT
 -> allowedActions = MANAGE_FAILED_EVENT, REQUEST_REPLAY_APPROVAL, REPLAY_FAILED_EVENT
 -> deniedActions = REVIEW_REPLAY_APPROVAL
```

Playwright 页面 smoke：

```text
打开 /failed-events.html
重放工作台角色切到 ORDER_SUPPORT
点击校验身份
检查按钮 disabled 状态
```

结果：

```json
{
  "status": "local-admin / ORDER_SUPPORT | 可3 禁1",
  "requestDisabled": false,
  "approveDisabled": true,
  "rejectDisabled": true,
  "replayDisabled": false
}
```

切回 `SRE` 后：

```json
{
  "status": "local-admin / SRE | 可4 禁0",
  "requestDisabled": false,
  "approveDisabled": false,
  "rejectDisabled": false,
  "replayDisabled": false
}
```

## Docker 和进程处理

本轮完整测试需要 Docker/Testcontainers：

```text
启动 Docker Desktop
确认 docker daemon version = 29.4.0
mvn test 跑完 RabbitMQ Testcontainers 集成测试
收尾阶段关闭本轮启动的 Docker Desktop
```

HTTP smoke 和 Playwright smoke 使用 jar：

```text
端口：18082
进程：PID 18840 / 25940
```

已在验证后停止。

## 清理结果

本轮收尾清理目标：

```text
停止 jar 冒烟进程
关闭 Playwright 浏览器会话
删除 target/v32-smoke.out.log、target/v32-smoke.err.log
删除 .playwright-cli 临时快照目录
关闭本轮启动的 Docker Desktop
确认无残留 java / Docker 进程
```

## 图片记录

```text
a/32/图片/01-version-32-change-summary.png
a/32/图片/02-code-test-v32.png
a/32/图片/03-full-test-v32.png
a/32/图片/04-http-playwright-smoke-v32.png
a/32/图片/05-cleanup-v32.png
```

## 本版结论

v32 把操作员动作权限快照真正落到页面按钮预检上。校验 `ORDER_SUPPORT` 后，审批按钮会在前端直接禁用；校验 `SRE` 后，重放工作台写按钮全部可用。后端授权仍然是最终防线，页面预检负责减少误操作和无意义 403。
