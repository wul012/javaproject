# 第四十四版：replay execution-contract blocked 稳定样本

## 计划依据

本版推进前重新读取最新计划：

```text
D:\nodeproj\orderops-node\docs\plans\v75-post-fixture-diagnostics-roadmap.md
```

计划对 Java v44 的目标是：

```text
新增一个 replay-execution-contract blocked sample，
覆盖 replayPreconditionsSatisfied=false、blockedBy 非空、executionChecks failed、
expectedSideEffects 为空或明确受限。
```

## 合理性判断

计划合理：

```text
1. Node v77 已完成 fixture evidence archive snapshot，下一步需要多场景样本。
2. Java v44 只新增 blocked 静态样本和测试，不新增写接口。
3. 样本只用于 Node v78 scenario matrix，不触发真实 replay POST。
4. 本版不修改 Node 或 mini-kv，边界清楚。
```

因此本版直接执行。

## 本版目标

新增 blocked 样本：

```text
src/main/resources/static/contracts/failed-event-replay-execution-contract-blocked.sample.json
```

应用启动后可读取：

```text
GET /contracts/failed-event-replay-execution-contract-blocked.sample.json
```

关键语义：

```text
approvalStatus=PENDING
requiredApprovalStatus=APPROVED
replayPreconditionsSatisfied=false
blockedBy=["REPLAY_APPROVAL_NOT_APPROVED"]
executionChecks[1].status=FAILED
expectedSideEffects=[]
nextAllowedActions=["REQUEST_REPLAY_APPROVAL"]
```

## 代码改动

新增静态 JSON：

```text
src/main/resources/static/contracts/failed-event-replay-execution-contract-blocked.sample.json
```

更新测试：

```text
src/test/java/com/codexdemo/orderplatform/FailedEventReplayExecutionContractIntegrationTests.java
```

更新文档：

```text
README.md
代码讲解记录/README.md
代码讲解记录/48-version-44-replay-execution-contract-blocked-sample.md
a/44/解释/说明.md
```

## 验证记录

聚焦测试：

```text
mvn "-Dtest=FailedEventReplayExecutionContractIntegrationTests" test
```

结果：

```text
Tests run: 4, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

全量测试：

```text
mvn test
```

结果：

```text
Tests run: 62, Failures: 0, Errors: 0, Skipped: 4
BUILD SUCCESS
```

说明：

```text
Docker 未运行，4 个 Testcontainers 用例按既有条件跳过；本版静态样本不需要 Docker。
```

打包验证：

```text
mvn package
```

结果：

```text
Tests run: 62, Failures: 0, Errors: 0, Skipped: 4
BUILD SUCCESS
生成 target/advanced-order-platform-0.1.0-SNAPSHOT.jar
```

HTTP smoke：

```text
D:\21jdk\bin\java.exe -jar target/advanced-order-platform-0.1.0-SNAPSHOT.jar --server.port=18086 --order.expiration.enabled=false --outbox.publisher.enabled=false
GET http://localhost:18086/actuator/health
GET http://localhost:18086/contracts/failed-event-replay-execution-contract-approved.sample.json
GET http://localhost:18086/contracts/failed-event-replay-execution-contract-blocked.sample.json
```

结果：

```text
health=UP
approved.replayPreconditionsSatisfied=True
approved.expectedSideEffectsCount=4
blocked.approvalStatus=PENDING
blocked.replayPreconditionsSatisfied=False
blocked.blockedBy=REPLAY_APPROVAL_NOT_APPROVED
blocked.executionCheckStatus=FAILED
blocked.expectedSideEffectsCount=0
blocked.nextAllowedAction=REQUEST_REPLAY_APPROVAL
port 18086 released
```

## 图片记录

截图目录：

```text
a/44/图片/
```

已保存：

```text
01-focused-test.png
02-full-test.png
03-package.png
04-http-smoke-blocked-sample.png
05-cleanup.png
```

## 边界

本版不做：

```text
不执行 replay
不申请 replay approval
不审批 replay approval
不写 replay attempt
不调用 RabbitMQ
不修改数据库结构
不修改 Node
不修改 mini-kv
不把样本当成生产数据
```

## 清理记录

已执行清理：

```text
mvn clean
Remove-Item tmp-v44-logs
```

结果：

```text
target exists=False
tmp-v44-logs exists=False
port 18086 in use=False
Java processes=0
```
