# 开发运行调试 v33：失败事件页面写操作本地权限守卫

## 本次目标

v33 控制成一个小闭环：

```text
v32 已经能按 allowedActions 禁用未授权按钮
v33 继续在写操作处理函数入口做本地权限守卫
```

目标不是替代后端鉴权，而是防止前端禁用按钮被脚本改回可点击后，仍然继续触发未授权写动作。

## 代码改动

页面脚本：

```text
src/main/resources/static/failed-events.js
```

新增本地权限快照：

```javascript
operatorPermissions: {
    management: { checked: false, allowedActions: [] },
    replay: { checked: false, allowedActions: [] }
}
```

身份校验成功后同时记住权限并应用按钮状态：

```javascript
rememberOperatorPermissions(scope, result.allowedActions || []);
applyOperatorPermissions(scope, result.allowedActions || []);
```

校验失败后记录为“已校验但无动作权限”：

```javascript
rememberOperatorPermissions(scope, []);
applyOperatorPermissions(scope, []);
```

新增入口守卫：

```javascript
function ensureOperatorActionAllowed(scope, action) {
    const permission = state.operatorPermissions[scope];
    if (!permission || !permission.checked) {
        return true;
    }
    if ((permission.allowedActions || []).includes(action)) {
        return true;
    }
    showToast(`${actionLabel(action)} 未授权，请切换角色后重新校验身份`, true);
    return false;
}
```

写操作入口全部先检查本地权限：

```javascript
if (!ensureOperatorActionAllowed("management", "MANAGE_FAILED_EVENT")) {
    return;
}
```

```javascript
if (!ensureOperatorActionAllowed("replay", "REQUEST_REPLAY_APPROVAL")) {
    return;
}
```

```javascript
if (!ensureOperatorActionAllowed("replay", "REVIEW_REPLAY_APPROVAL")) {
    return;
}
```

```javascript
if (!ensureOperatorActionAllowed("replay", "REPLAY_FAILED_EVENT")) {
    return;
}
```

切换操作人或角色后清空旧快照：

```javascript
state.operatorPermissions[scope] = { checked: false, allowedActions: [] };
```

页面静态测试：

```text
src/test/java/com/codexdemo/orderplatform/FailedEventManagementPageTests.java
```

新增断言：

```java
"operatorPermissions",
"rememberOperatorPermissions",
"ensureOperatorActionAllowed",
```

## 文档改动

```text
README.md
代码讲解记录/37-version-33-page-action-guard.md
代码讲解记录/README.md
a/33/解释/说明.md
```

## 已执行验证

基础检查：

```powershell
mvn -DskipTests compile
node --check src/main/resources/static/failed-events.js
```

结果：

```text
compile -> BUILD SUCCESS
node --check -> OK
```

相关测试：

```powershell
mvn "-Dtest=FailedEventManagementPageTests,FailedEventOperatorContextIntegrationTests" test
```

结果：

```text
Tests run: 3, Failures: 0, Errors: 0, Skipped: 0
```

完整测试：

```powershell
mvn test
```

结果：

```text
Tests run: 33, Failures: 0, Errors: 0, Skipped: 0
```

打包：

```powershell
mvn -DskipTests package
```

结果：

```text
BUILD SUCCESS
```

HTTP smoke：

```text
/actuator/health -> UP
/failed-events.html -> 200
/operator-context + ORDER_SUPPORT
 -> allowedActions = MANAGE_FAILED_EVENT, REQUEST_REPLAY_APPROVAL, REPLAY_FAILED_EVENT
 -> deniedActions = REVIEW_REPLAY_APPROVAL
```

Playwright 页面 smoke：

```text
打开 /failed-events.html
重放工作台角色切到 ORDER_SUPPORT
点击校验身份
检查按钮 disabled 状态
脚本强行打开 Approve 按钮
直接调用 reviewReplayApproval("APPROVED")
```

按钮状态：

```json
{
  "status": "local-admin / ORDER_SUPPORT | 可3 禁1",
  "approveDisabled": true,
  "rejectDisabled": true,
  "requestDisabled": false,
  "replayDisabled": false
}
```

绕过按钮禁用后的本地守卫结果：

```json
{
  "toast": "审批 未授权，请切换角色后重新校验身份",
  "disabled": false
}
```

## Docker 和进程处理

本轮完整测试需要 Docker/Testcontainers：

```text
启动 Docker Desktop
确认 docker daemon version = 29.4.0
mvn test 跑完 RabbitMQ Testcontainers 集成测试
收尾阶段关闭本轮启动的 Docker Desktop
```

HTTP smoke 和 Playwright smoke 使用 jar：

```text
端口：18083
```

已在验证后停止。

## 清理结果

本轮收尾清理目标：

```text
停止 jar 冒烟进程
关闭 Playwright 浏览器会话
删除 target/v33-smoke.out.log、target/v33-smoke.err.log
删除 .playwright-cli 临时快照目录
关闭本轮启动的 Docker Desktop
确认无残留 java / Docker 进程
```

## 图片记录

```text
a/33/图片/01-version-33-change-summary.png
a/33/图片/02-code-test-v33.png
a/33/图片/03-full-test-v33.png
a/33/图片/04-http-playwright-smoke-v33.png
a/33/图片/05-cleanup-v33.png
```

## 本版结论

v33 把页面权限预检从“禁用按钮”推进到“写操作入口也会守卫”。`ORDER_SUPPORT` 校验后，即使脚本把审批按钮改回可点击，`reviewReplayApproval("APPROVED")` 仍会先提示未授权并停止，不会继续发起审批写请求。
