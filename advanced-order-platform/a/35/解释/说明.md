# 开发运行调试 v35：失败事件动作级角色策略启动期校验

## 本次目标

v35 控制成一个清晰小闭环：

```text
失败事件动作级角色策略
 -> 启动期做一致性校验
 -> 配置错了直接 fail fast
```

这版不新增接口、不改页面、不改数据库，只补权限配置的工程可靠性。

## 代码改动

配置类：

```text
src/main/java/com/codexdemo/orderplatform/notification/FailedEventReplayProperties.java
```

新增 `@PostConstruct` 校验：

```java
@PostConstruct
void validateRolePolicy() {
    Set<String> globalAllowedRoles = normalizedRoleSet("allowed-roles", allowedRoles);
    List<String> errors = new ArrayList<>();
    validateActionRoles("management-roles", managementRoles, globalAllowedRoles, errors);
    validateActionRoles("replay-approval-request-roles", replayApprovalRequestRoles, globalAllowedRoles, errors);
    validateActionRoles("replay-approval-review-roles", replayApprovalReviewRoles, globalAllowedRoles, errors);
    Set<String> normalizedReplayRoles = validateActionRoles("replay-roles", replayRoles, globalAllowedRoles, errors);
    String normalizedSystemRole = normalizeRequiredRole("system-role", systemRole, errors);
    ...
}
```

校验规则：

```text
allowed-roles
 -> 至少包含一个非空角色

management-roles
replay-approval-request-roles
replay-approval-review-roles
replay-roles
 -> 至少包含一个非空角色
 -> 不能包含 allowed-roles 之外的角色

system-role
 -> 不能为空
 -> 必须包含在 allowed-roles
 -> 必须包含在 replay-roles
```

动作角色越界时错误示例：

```text
invalid failed-event.replay role policy:
replay-approval-request-roles contains roles outside allowed-roles: ORDER_SUPPORT
```

系统角色不能重放时错误示例：

```text
invalid failed-event.replay role policy:
system-role AUTOMATION must be included in replay-roles
```

新增单元测试：

```text
src/test/java/com/codexdemo/orderplatform/notification/FailedEventReplayPropertiesTests.java
```

覆盖：

```text
默认角色策略通过
动作角色不能越过 allowed-roles
system-role 必须能执行重放
allowed-roles 不能为空
```

## 文档改动

```text
README.md
代码讲解记录/39-version-35-role-policy-validation.md
代码讲解记录/README.md
a/35/解释/说明.md
```

## 已执行验证

编译：

```powershell
mvn -DskipTests compile
```

结果：

```text
BUILD SUCCESS
```

相关测试：

```powershell
mvn "-Dtest=FailedEventReplayPropertiesTests,FailedEventOperatorContextIntegrationTests" test
```

结果：

```text
Tests run: 6, Failures: 0, Errors: 0, Skipped: 0
```

完整测试：

```powershell
mvn test
```

结果：

```text
Tests run: 37, Failures: 0, Errors: 0, Skipped: 0
```

打包：

```powershell
mvn -DskipTests package
```

结果：

```text
BUILD SUCCESS
```

HTTP smoke：

```text
/actuator/health -> UP
/failed-events.html -> 200
/operator-context + SYSTEM
 -> actionDecisions.REPLAY_FAILED_EVENT.allowed = true
 -> deniedActions = empty
/operator-context + ORDER_SUPPORT
 -> actionDecisions.REVIEW_REPLAY_APPROVAL.allowed = false
 -> deniedActions = REVIEW_REPLAY_APPROVAL
```

## Docker 和进程处理

本轮完整测试需要 Docker/Testcontainers：

```text
启动 Docker Desktop
确认 docker daemon version = 29.4.0
mvn test 跑完 RabbitMQ Testcontainers 集成测试
收尾阶段关闭本轮启动的 Docker Desktop
```

HTTP smoke 使用 jar：

```text
端口：18085
```

已在验证后停止。

## 清理结果

本轮收尾清理目标：

```text
停止 jar 冒烟进程
删除 target/v35-smoke.out.log、target/v35-smoke.err.log
删除 target/v35-started-docker.flag
关闭本轮启动的 Docker Desktop
确认无残留 java / Docker 进程
```

## 图片记录

```text
a/35/图片/01-version-35-change-summary.png
a/35/图片/02-policy-validation-v35.png
a/35/图片/03-policy-tests-v35.png
a/35/图片/04-http-smoke-v35.png
a/35/图片/05-cleanup-v35.png
```

## 本版结论

v35 把失败事件动作级角色配置从“运行时靠权限判断兜底”升级为“启动期先做一致性校验”。如果动作角色越过 `allowed-roles`，或者 `system-role` 不能执行重放，应用会直接启动失败，减少后续排查成本。
