# 开发运行调试 v37：失败事件治理摘要

## 计划依据和合理性判断

本版依据最新计划：

```text
D:\nodeproj\orderops-node\docs\plans\v54-post-infojson-roadmap.md
```

计划建议下一阶段第一批推进：

```text
Java v37：失败事件治理摘要
mini-kv v46：COMMANDS / COMMANDSJSON 命令分级
```

如果只先做一个，计划明确建议：

```text
优先 Java v37
```

合理性判断：

```text
合理，可以执行。
```

原因：

```text
本版只新增 Java 只读接口
不执行 replay
不审批 replay
不接 mini-kv
不修改 Node
不会跨多个项目核心层
```

## 本次目标

新增失败事件治理摘要接口：

```text
GET /api/v1/failed-events/summary
```

用于让 Java 只读告诉 Node：

```text
失败事件当前有多少积压
多少待审批
多少已批准/已拒绝
最近失败发生在什么时候
最近审批活动发生在什么时候
```

## 返回字段

```text
sampledAt
totalFailedEvents
pendingReplayApprovals
approvedReplayApprovals
rejectedReplayApprovals
latestFailedAt
latestApprovalAt
replayBacklog
```

`replayBacklog` 的定义：

```text
status != REPLAYED 的失败事件数量
```

也就是尚未成功完成重放闭环的失败事件数量。

## 代码改动

新增响应对象：

```text
src/main/java/com/codexdemo/orderplatform/notification/FailedEventSummaryResponse.java
```

```java
public record FailedEventSummaryResponse(
        Instant sampledAt,
        long totalFailedEvents,
        long pendingReplayApprovals,
        long approvedReplayApprovals,
        long rejectedReplayApprovals,
        Instant latestFailedAt,
        Instant latestApprovalAt,
        long replayBacklog
) {
}
```

新增只读聚合服务：

```text
src/main/java/com/codexdemo/orderplatform/notification/FailedEventSummaryService.java
```

核心聚合：

```java
@Transactional(readOnly = true)
public FailedEventSummaryResponse summary() {
    return new FailedEventSummaryResponse(
            Instant.now(),
            failedEventMessageRepository.count(),
            failedEventMessageRepository.countByReplayApprovalStatus(FailedEventReplayApprovalStatus.PENDING),
            failedEventMessageRepository.countByReplayApprovalStatus(FailedEventReplayApprovalStatus.APPROVED),
            failedEventMessageRepository.countByReplayApprovalStatus(FailedEventReplayApprovalStatus.REJECTED),
            latestFailedAt(),
            latestApprovalAt(),
            failedEventMessageRepository.countByStatusNot(FailedEventMessageStatus.REPLAYED)
    );
}
```

新增 HTTP 入口：

```text
src/main/java/com/codexdemo/orderplatform/notification/FailedEventSummaryController.java
```

```java
@GetMapping("/summary")
public FailedEventSummaryResponse summary() {
    return failedEventSummaryService.summary();
}
```

补充 repository 派生查询：

```text
src/main/java/com/codexdemo/orderplatform/notification/FailedEventMessageRepository.java
```

```java
long countByStatusNot(FailedEventMessageStatus status);

Optional<FailedEventMessage> findTopByReplayApprovalRequestedAtIsNotNullOrderByReplayApprovalRequestedAtDescIdDesc();

Optional<FailedEventMessage> findTopByReplayApprovalReviewedAtIsNotNullOrderByReplayApprovalReviewedAtDescIdDesc();
```

## 测试补充

新增服务单测：

```text
src/test/java/com/codexdemo/orderplatform/notification/FailedEventSummaryServiceTests.java
```

覆盖：

```text
总失败事件数
PENDING / APPROVED / REJECTED 审批状态计数
latestFailedAt
latestApprovalAt 从 request/review 中取最新
replayBacklog
```

新增 HTTP 集成测试：

```text
src/test/java/com/codexdemo/orderplatform/FailedEventSummaryIntegrationTests.java
```

覆盖：

```text
GET /api/v1/failed-events/summary
 -> 200 OK
 -> totalFailedEvents = 4
 -> pendingReplayApprovals = 1
 -> approvedReplayApprovals = 1
 -> rejectedReplayApprovals = 1
 -> latestApprovalAt = 2026-05-11T08:20:00Z
 -> replayBacklog = 3
```

## 验证记录

v37 相关测试：

```text
mvn "-Dtest=FailedEventSummaryServiceTests,FailedEventSummaryIntegrationTests" test
 -> Tests run: 2, Failures: 0, Errors: 0, Skipped: 0
 -> BUILD SUCCESS
```

全量测试：

```text
mvn test
 -> Tests run: 41, Failures: 0, Errors: 0, Skipped: 4
 -> BUILD SUCCESS
```

说明：

```text
4 个跳过项是 Testcontainers PostgreSQL/RabbitMQ 集成测试。
当前 Docker 环境不可用，测试类使用 @Testcontainers(disabledWithoutDocker = true) 自动跳过。
```

打包：

```text
mvn package
 -> Tests run: 41, Failures: 0, Errors: 0, Skipped: 4
 -> target/advanced-order-platform-0.1.0-SNAPSHOT.jar
 -> BUILD SUCCESS
```

HTTP smoke：

```text
java -jar target/advanced-order-platform-0.1.0-SNAPSHOT.jar --server.port=18081 --order.expiration.enabled=false --outbox.publisher.enabled=false
Invoke-RestMethod http://127.0.0.1:18081/actuator/health
Invoke-RestMethod http://127.0.0.1:18081/api/v1/failed-events/summary
```

结果：

```text
health=UP
totalFailedEvents=0
pendingReplayApprovals=0
approvedReplayApprovals=0
rejectedReplayApprovals=0
latestFailedAt=null
latestApprovalAt=null
replayBacklog=0
```

smoke 清理：

```text
PID 36884 stopped
port 18081 released
```

最终清理：

```text
mvn clean
 -> target build output, jar and smoke logs removed
 -> BUILD SUCCESS
```

## 文档归档

README 已补充：

```text
当前能力
API Quick Start
Architecture Direction / notification v37
```

代码讲解记录已补充：

```text
代码讲解记录/41-version-37-failed-event-summary.md
代码讲解记录/README.md
```

图片记录：

```text
a/37/图片/01-typecheck-or-build.png
a/37/图片/02-test-result.png
a/37/图片/03-smoke-result.png
a/37/图片/04-endpoint-json.png
a/37/图片/05-cleanup.png
```

## 一句话总结

v37 把失败事件治理从“能查明细”推进到“能看摘要”，让后续 Node v55 可以只读消费 Java 的失败事件积压、审批状态和治理活动时间。
