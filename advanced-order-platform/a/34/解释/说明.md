# 开发运行调试 v34：失败事件操作员动作权限决策明细

## 本次目标

v34 控制成一个中等偏小闭环：

```text
后端 operator-context 返回每个动作的权限决策明细
页面把当前区域动作渲染成允许/禁止标签
```

这版不新增数据库结构，不改变授权规则，只提升权限探针的解释能力和页面可读性。

## 代码改动

新增动作决策响应对象：

```text
src/main/java/com/codexdemo/orderplatform/notification/FailedEventOperatorActionDecision.java
```

核心结构：

```java
public record FailedEventOperatorActionDecision(
        FailedEventOperatorAction action,
        boolean allowed,
        List<String> allowedRoles
) {
}
```

Resolver 新增决策表生成：

```text
src/main/java/com/codexdemo/orderplatform/notification/FailedEventOperatorContextResolver.java
```

核心代码：

```java
public Map<FailedEventOperatorAction, FailedEventOperatorActionDecision> actionDecisionsFor(String operatorRole) {
    String normalizedRole = failedEventReplayProperties.normalize(operatorRole);
    Map<FailedEventOperatorAction, FailedEventOperatorActionDecision> decisions =
            new EnumMap<>(FailedEventOperatorAction.class);
    for (FailedEventOperatorAction action : FailedEventOperatorAction.values()) {
        decisions.put(action, new FailedEventOperatorActionDecision(
                action,
                failedEventReplayProperties.isAllowedFor(action, normalizedRole),
                normalizeRoles(failedEventReplayProperties.rolesFor(action))
        ));
    }
    return decisions;
}
```

`/operator-context` 响应新增字段：

```text
src/main/java/com/codexdemo/orderplatform/notification/FailedEventOperatorContextResponse.java
src/main/java/com/codexdemo/orderplatform/notification/FailedEventMessageController.java
```

新增字段：

```java
Map<FailedEventOperatorAction, FailedEventOperatorActionDecision> actionDecisions,
```

页面新增两个决策展示区：

```text
src/main/resources/static/failed-events.html
```

```html
<div id="operatorActionDecisionList" class="action-decision-list" aria-live="polite">
    <span class="action-decision-empty">未校验</span>
</div>
```

```html
<div id="replayActionDecisionList" class="action-decision-list replay-action-decisions" aria-live="polite">
    <span class="action-decision-empty">未校验</span>
</div>
```

页面渲染函数：

```text
src/main/resources/static/failed-events.js
```

核心逻辑：

```javascript
function renderOperatorActionDecisions(scope, result, fallbackText = "未校验") {
    const listElement = actionDecisionListElement(scope);
    if (!result) {
        listElement.innerHTML = `<span class="action-decision-empty">${escapeHtml(fallbackText)}</span>`;
        return;
    }
    const allowedActionSet = new Set(result.allowedActions || []);
    const decisions = result.actionDecisions || {};
    const rolesByAction = result.allowedRolesByAction || {};
    listElement.innerHTML = actionControls(scope).map(({ action }) => {
        const decision = decisions[action] || {};
        const allowed = typeof decision.allowed === "boolean" ? decision.allowed : allowedActionSet.has(action);
        const allowedRoles = decision.allowedRoles || rolesByAction[action] || [];
        ...
    }).join("");
}
```

样式：

```text
src/main/resources/static/failed-events.css
```

允许和禁止标签：

```css
.action-decision-allowed {
    border-color: #a9d8b4;
    background: #edf8f0;
}

.action-decision-denied {
    border-color: #e5b4b4;
    border-style: dashed;
    background: #fff1f1;
}
```

测试更新：

```text
src/test/java/com/codexdemo/orderplatform/FailedEventOperatorContextIntegrationTests.java
src/test/java/com/codexdemo/orderplatform/FailedEventManagementPageTests.java
```

## 文档改动

```text
README.md
代码讲解记录/38-version-34-action-decision-summary.md
代码讲解记录/README.md
a/34/解释/说明.md
```

## 已执行验证

JS 语法检查：

```powershell
node --check src/main/resources/static/failed-events.js
```

结果：

```text
OK
```

编译：

```powershell
mvn -DskipTests compile
```

结果：

```text
BUILD SUCCESS
```

相关测试：

```powershell
mvn "-Dtest=FailedEventManagementPageTests,FailedEventOperatorContextIntegrationTests" test
```

结果：

```text
Tests run: 3, Failures: 0, Errors: 0, Skipped: 0
```

完整测试：

```powershell
mvn test
```

结果：

```text
Tests run: 33, Failures: 0, Errors: 0, Skipped: 0
```

打包：

```powershell
mvn -DskipTests package
```

结果：

```text
BUILD SUCCESS
```

HTTP smoke：

```text
/actuator/health -> UP
/failed-events.html -> 200
/operator-context + ORDER_SUPPORT
 -> actionDecisions.REQUEST_REPLAY_APPROVAL.allowed = true
 -> actionDecisions.REVIEW_REPLAY_APPROVAL.allowed = false
 -> actionDecisions.REVIEW_REPLAY_APPROVAL.allowedRoles = SRE/SYSTEM
```

Playwright 页面 smoke：

```text
打开 /failed-events.html
重放工作台角色切到 ORDER_SUPPORT
点击校验身份
读取 replayActionDecisionList 标签和审批按钮状态
```

结果：

```json
{
  "status": "local-admin / ORDER_SUPPORT | 可3 禁1",
  "chips": [
    "申请 允许 ORDER_SUPPORT/SRE/SYSTEM",
    "审批 禁止 SRE/SYSTEM",
    "重放 允许 ORDER_SUPPORT/SRE/SYSTEM"
  ],
  "deniedCount": 1,
  "approveDisabled": true
}
```

## Docker 和进程处理

本轮完整测试需要 Docker/Testcontainers：

```text
启动 Docker Desktop
确认 docker daemon version = 29.4.0
mvn test 跑完 RabbitMQ Testcontainers 集成测试
收尾阶段关闭本轮启动的 Docker Desktop
```

HTTP smoke 和 Playwright smoke 使用 jar：

```text
端口：18084
```

已在验证后停止。

## 清理结果

本轮收尾清理目标：

```text
停止 jar 冒烟进程
关闭 Playwright 浏览器会话
删除 target/v34-smoke.out.log、target/v34-smoke.err.log
删除 target/v34-started-docker.flag
删除 .playwright-cli 临时快照目录
关闭本轮启动的 Docker Desktop
确认无残留 java / Docker 进程
```

## 图片记录

```text
a/34/图片/01-version-34-change-summary.png
a/34/图片/02-backend-decision-v34.png
a/34/图片/03-ui-decision-v34.png
a/34/图片/04-test-smoke-v34.png
a/34/图片/05-cleanup-v34.png
```

## 本版结论

v34 让失败事件操作员权限从“可执行动作列表”升级为“动作级决策明细”。后端响应能直接说明每个动作是否允许、允许角色是什么；页面也能把 `ORDER_SUPPORT` 的“审批 禁止 SRE/SYSTEM”展示出来，和按钮禁用、写操作入口守卫形成更清晰的权限闭环。
