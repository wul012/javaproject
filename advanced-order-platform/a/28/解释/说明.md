# 开发运行调试 v28：重放审批职责分离

## 本次目标

v27 已经把失败事件重放审批升级成可查询、可导出的历史流水。v28 继续补一条生产系统里很常见的内控规则：

```text
申请人不能审批自己的重放申请
```

这条规则让审批不只是“流程存在”，而是开始具备基本职责分离。

## 代码改动

服务层新增自提自审拦截：
```text
src/main/java/com/codexdemo/orderplatform/notification/FailedEventMessageService.java
```

核心逻辑：
```java
ensureReplayApprovalReviewerIsDifferent(failedMessage, normalizedOperatorId);
```

方法实现：
```java
private void ensureReplayApprovalReviewerIsDifferent(FailedEventMessage failedMessage, String reviewerId) {
    String requesterId = failedMessage.getReplayApprovalRequestedBy();
    if (requesterId != null && requesterId.equals(reviewerId)) {
        throw new ResponseStatusException(
                HttpStatus.CONFLICT,
                "replay approval requester cannot review own request"
        );
    }
}
```

前端新增提交前提示：
```text
src/main/resources/static/failed-events.js
```

核心逻辑：
```javascript
if (isSelfReviewAttempt()) {
    showToast("申请人不能审批自己的重放申请，请切换审批人", true);
    return;
}
```

判断函数：
```javascript
function isSelfReviewAttempt() {
    const event = state.activeEvent || {};
    return event.replayApprovalStatus === "PENDING"
            && event.replayApprovalRequestedBy
            && event.replayApprovalRequestedBy === elements.replayOperatorIdInput.value.trim();
}
```

## 文档改动

```text
README.md
代码讲解记录/32-version-28-replay-approval-separation.md
代码讲解记录/README.md
```

## 自动化调整

你要求自动化改成“每隔一段时间重复检测一次，当前版本没完成推进当前的，完成了继续往后，推荐半小时重复检测一次”。

已处理：
```text
保留当前线程 heartbeat 自动化
rrule = FREQ=MINUTELY;INTERVAL=30
删除重复的每天 09:00 cron 自动化
```

新的策略：
```text
每 30 分钟检查一次
如果当前版本未完成，继续当前版本
如果当前版本已完成、已提交、已打 tag、已推送，推进下一版本
```

## 已执行验证

基础检查：
```powershell
mvn -DskipTests compile
node --check src/main/resources/static/failed-events.js
```

结果：
```text
compile -> BUILD SUCCESS
node --check -> OK
```

相关测试：
```powershell
mvn "-Dtest=FailedEventSearchIntegrationTests,FailedEventManagementPageTests" test
```

结果：
```text
Tests run: 8, Failures: 0, Errors: 0, Skipped: 0
```

完整测试：
```powershell
mvn test
```

结果：
```text
Tests run: 31, Failures: 0, Errors: 0, Skipped: 0
```

打包：
```powershell
mvn -DskipTests package
```

结果：
```text
BUILD SUCCESS
```

jar HTTP 冒烟：
```text
/actuator/health -> UP
/failed-events.html -> 200
临时失败事件申请审批 -> PENDING
同一 smoke-user 审批自己的申请 -> HTTP 409
审批历史 -> 仍只有 REQUESTED 一条
```

## 清理结果

本轮收尾清理：
```text
停止 jar 冒烟进程：PID 29328
删除临时 SQL 和日志：target/v28-smoke-data.sql、target/v28-smoke.out.log、target/v28-smoke.err.log
Testcontainers 无残留容器
Docker Desktop 已关闭
```

## 图片记录

```text
a/28/图片/01-version-28-change-summary.png
a/28/图片/02-code-test-v28.png
a/28/图片/03-full-test-v28.png
a/28/图片/04-http-smoke-v28.png
a/28/图片/05-automation-cleanup-v28.png
```

## 本版结论

v28 给失败事件重放审批补上了职责分离：申请人不能审批自己的申请。现在审批链路不仅能申请、拒绝、批准和追溯历史，还能避免最基础的自提自审风险。
