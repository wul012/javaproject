# 开发运行调试 v36：订单平台只读运行概览

## 本次目标

v36 按三项目综合路线图推进第一步：

```text
Java 先补稳定、只读、面向控制面的业务概览接口
 -> Node 后续可以接入 Java 业务信号
 -> mini-kv 暂不参与
```

本版只做一个小闭环：

```text
GET /api/v1/ops/overview
 -> application
 -> orders
 -> inventory
 -> outbox
 -> failedEvents
```

## 本版边界

```text
不触发失败事件重放
不修改订单、库存、Outbox 或失败事件状态
不接入 mini-kv
不引入真实登录系统
不修改 Node 项目
```

## 代码改动

新增只读运行概览响应对象：

```text
src/main/java/com/codexdemo/orderplatform/ops/OpsOverviewResponse.java
```

核心结构：

```java
public record OpsOverviewResponse(
        Instant sampledAt,
        Application application,
        Orders orders,
        Inventory inventory,
        Outbox outbox,
        FailedEvents failedEvents
) {
```

新增聚合服务：

```text
src/main/java/com/codexdemo/orderplatform/ops/OpsOverviewService.java
```

服务层使用只读事务，并只调用 repository 查询：

```java
@Transactional(readOnly = true)
public OpsOverviewResponse overview() {
    Instant sampledAt = Instant.now();
    return new OpsOverviewResponse(
            sampledAt,
            application(sampledAt),
            new OpsOverviewResponse.Orders(orderRepository.count()),
            new OpsOverviewResponse.Inventory(inventoryRepository.count()),
            new OpsOverviewResponse.Outbox(outboxRepository.countByPublishedAtIsNull()),
            failedEvents()
    );
}
```

新增 HTTP 入口：

```text
src/main/java/com/codexdemo/orderplatform/ops/OpsOverviewController.java
```

接口路径：

```java
@GetMapping("/overview")
public OpsOverviewResponse overview() {
    return opsOverviewService.overview();
}
```

失败事件仓储补充两个只读查询：

```text
src/main/java/com/codexdemo/orderplatform/notification/FailedEventMessageRepository.java
```

```java
long countByReplayApprovalStatus(FailedEventReplayApprovalStatus replayApprovalStatus);

Optional<FailedEventMessage> findTopByOrderByFailedAtDescIdDesc();
```

## 返回结构

HTTP smoke 返回的核心字段如下：

```json
{
  "health": "UP",
  "overviewApplication": "advanced-order-platform",
  "profiles": "default",
  "ordersTotal": 0,
  "inventoryItems": 3,
  "outboxPending": 0,
  "failedEventsTotal": 0,
  "pendingReplayApprovals": 0,
  "latestFailedAt": null
}
```

## 测试补充

新增聚合单测：

```text
src/test/java/com/codexdemo/orderplatform/ops/OpsOverviewServiceTests.java
```

覆盖：

```text
orderRepository.count()
inventoryRepository.count()
outboxRepository.countByPublishedAtIsNull()
failedEventMessageRepository.count()
failedEventMessageRepository.countByReplayApprovalStatus(PENDING)
```

新增 HTTP 集成测试：

```text
src/test/java/com/codexdemo/orderplatform/OpsOverviewIntegrationTests.java
```

覆盖：

```text
GET /api/v1/ops/overview
 -> 200 OK
 -> application.name
 -> application.profiles
 -> orders.total
 -> inventory.items
 -> outbox.pending
 -> failedEvents.total
 -> failedEvents.pendingReplayApprovals
 -> failedEvents.latestFailedAt
```

## 验证记录

v36 相关测试：

```text
mvn "-Dtest=OpsOverviewServiceTests,OpsOverviewIntegrationTests" test
 -> Tests run: 2, Failures: 0, Errors: 0, Skipped: 0
 -> BUILD SUCCESS
```

全量测试：

```text
mvn test
 -> Tests run: 39, Failures: 0, Errors: 0, Skipped: 4
 -> BUILD SUCCESS
```

说明：4 个跳过项是 Testcontainers PostgreSQL/RabbitMQ 集成测试，当前 Docker 环境不可用，测试类使用 `@Testcontainers(disabledWithoutDocker = true)` 自动跳过。

打包：

```text
mvn package
 -> Tests run: 39, Failures: 0, Errors: 0, Skipped: 4
 -> target/advanced-order-platform-0.1.0-SNAPSHOT.jar
 -> BUILD SUCCESS
```

HTTP smoke：

```text
java -jar target/advanced-order-platform-0.1.0-SNAPSHOT.jar --server.port=18080 --order.expiration.enabled=false --outbox.publisher.enabled=false
Invoke-RestMethod http://127.0.0.1:18080/actuator/health
Invoke-RestMethod http://127.0.0.1:18080/api/v1/ops/overview
```

结果：

```text
health=UP
overviewApplication=advanced-order-platform
profiles=default
ordersTotal=0
inventoryItems=3
outboxPending=0
failedEventsTotal=0
pendingReplayApprovals=0
```

smoke 进程：

```text
PID 32976 stopped
port 18080 released
```

清理：

```text
mvn clean
 -> target build output, jar and smoke logs removed
 -> BUILD SUCCESS
```

## 文档归档

README 已补充：

```text
当前能力
API Quick Start
Architecture Direction / ops
```

代码讲解记录已补充：

```text
代码讲解记录/40-version-36-ops-overview.md
代码讲解记录/README.md
```

图片记录：

```text
a/36/图片/01-version-36-change-summary.png
a/36/图片/02-ops-overview-tests-v36.png
a/36/图片/03-full-test-v36.png
a/36/图片/04-package-http-smoke-v36.png
a/36/图片/05-cleanup-v36.png
```

## 一句话总结

v36 把 Java 订单平台从“技术健康检查”推进到“业务运行概览”，但仍保持只读、松耦合和小版本边界，为后续 Node v53 接入 Java business signals 做好准备。
