# 开发运行调试 v27：失败事件重放审批历史流水

## 本次目标

v25 已经有“申请审批、审批通过/拒绝、通过后才能重放”的门禁，但审批信息主要保存在 `failed_event_messages` 当前行上。

v27 补齐审批流水表，让每一次申请、拒绝、再次申请、批准都能长期追溯。

## 主要改动

新增审批历史模型：
```text
FailedEventReplayApprovalHistory
FailedEventReplayApprovalHistoryAction
FailedEventReplayApprovalHistoryRepository
FailedEventReplayApprovalHistoryResponse
FailedEventReplayApprovalHistorySearchCriteria
```

新增数据库迁移：
```text
src/main/resources/db/migration/h2/V11__failed_event_replay_approval_history.sql
src/main/resources/db/migration/postgresql/V11__failed_event_replay_approval_history.sql
```

新增接口：
```text
GET /api/v1/failed-events/{id}/replay-approval-history
GET /api/v1/failed-events/replay-approval-history
GET /api/v1/failed-events/replay-approval-history/export
```

页面增强：
```text
失败事件管理页重放工作台新增 Approval history 面板
申请/审批后自动刷新审批流水
```

## 核心代码点

申请审批时写当前状态和历史流水：
```java
failedMessage.requestReplayApproval(reason, normalizedOperatorId, requestedAt);
failedEventReplayApprovalHistoryRepository.save(FailedEventReplayApprovalHistory.record(
        failedMessage,
        FailedEventReplayApprovalHistoryAction.REQUESTED,
        normalizedOperatorId,
        normalizedOperatorRole,
        reason,
        requestedAt
));
```

审批通过或拒绝时写历史：
```java
failedEventReplayApprovalHistoryRepository.save(FailedEventReplayApprovalHistory.record(
        failedMessage,
        FailedEventReplayApprovalHistoryAction.valueOf(reviewStatus.name()),
        normalizedOperatorId,
        normalizedOperatorRole,
        note,
        reviewedAt
));
```

前端选择失败事件时同时加载三类信息：
```javascript
await Promise.all([
    loadHistory(id),
    loadReplayApprovalHistory(id),
    loadReplayAttempts(id)
]);
```

## Docker 说明

你问“Docker 需要我启动吗，你不能启动？”

这次我已自动启动 Docker Desktop：
```text
Started Docker Desktop: PID 28956
Docker API ready: ServerVersion=29.4.0
```

结论是：正常情况下我可以启动 Docker；只有遇到 Docker Desktop 更新、登录、权限弹窗、WSL 异常等必须桌面交互的情况，才需要你配合点击。

## 已执行验证

基础编译和脚本检查：
```powershell
mvn -DskipTests compile
node --check src/main/resources/static/failed-events.js
```

结果：
```text
compile -> BUILD SUCCESS
node --check -> OK
```

Docker 启动前的完整测试：
```powershell
mvn test
```

结果：
```text
Tests run: 31, Failures: 0, Errors: 0, Skipped: 4
```

说明：当时 Docker 未运行，4 个 Testcontainers 测试按配置跳过。

Docker 启动后补跑真实中间件测试：
```powershell
mvn "-Dtest=PostgresMigrationIntegrationTests,RabbitMqNotificationConsumerIntegrationTests,RabbitMqNotificationFailureIntegrationTests,RabbitMqOutboxPublisherIntegrationTests" test
```

结果：
```text
Tests run: 4, Failures: 0, Errors: 0, Skipped: 0
```

Docker 启动后的完整测试：
```powershell
mvn test
```

结果：
```text
Tests run: 31, Failures: 0, Errors: 0, Skipped: 0
```

打包：
```powershell
mvn -DskipTests package
```

结果：
```text
BUILD SUCCESS
```

jar HTTP 冒烟：
```text
/actuator/health -> UP
/failed-events.html -> 200，并包含 Approval history
/api/v1/failed-events/replay-approval-history?limit=5 -> totalElements=0, sort=changedAt,desc
/api/v1/failed-events?limit=5 -> totalElements=0, sort=failedAt,desc
```

## 清理结果

```text
停止 jar 冒烟进程：PID 3772
删除临时日志：target/v27-smoke.out.log、target/v27-smoke.err.log
Testcontainers 无残留容器
Docker Desktop 已关闭
Docker status after shutdown: stopped
```

## 图片记录

```text
a/27/图片/01-version-27-change-summary.png
a/27/图片/02-approval-history-code-tests-v27.png
a/27/图片/03-full-test-docker-v27.png
a/27/图片/04-http-smoke-v27.png
a/27/图片/05-cleanup-v27.png
```

## 本版结论

v27 把失败事件重放审批从“只看当前审批状态”推进到“完整审批流水可查、可筛选、可导出、页面可见”。这让失败事件重放链路更接近真实生产系统的审计要求。
