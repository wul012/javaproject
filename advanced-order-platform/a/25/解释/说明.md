# 开发运行调试 v25：失败事件重放审批门禁

## 本次目标

v25 在 v24 的“重放前二次确认”基础上继续加一层生产级门禁：失败事件必须先申请重放审批，审批通过后才能真正调用重放接口。

完整操作链路：
```text
选择失败事件
 -> request replay approval
 -> approve / reject
 -> APPROVED 后才允许打开重放确认
 -> 确认后 POST /api/v1/failed-events/{id}/replay
 -> 后端服务层再次检查审批状态
```

## 主要改动

```text
新增审批枚举：
src/main/java/com/codexdemo/orderplatform/notification/FailedEventReplayApprovalStatus.java

新增请求体：
src/main/java/com/codexdemo/orderplatform/notification/RequestFailedEventReplayApprovalRequest.java
src/main/java/com/codexdemo/orderplatform/notification/ReviewFailedEventReplayApprovalRequest.java

扩展失败事件实体：
src/main/java/com/codexdemo/orderplatform/notification/FailedEventMessage.java

新增审批接口和服务方法：
src/main/java/com/codexdemo/orderplatform/notification/FailedEventMessageController.java
src/main/java/com/codexdemo/orderplatform/notification/FailedEventMessageService.java

新增 Flyway V10：
src/main/resources/db/migration/h2/V10__failed_event_replay_approval.sql
src/main/resources/db/migration/postgresql/V10__failed_event_replay_approval.sql

扩展管理页面：
src/main/resources/static/failed-events.html
src/main/resources/static/failed-events.js
src/main/resources/static/failed-events.css
```

## 关键代码记录

实体默认审批状态：
```java
this.replayApprovalStatus = FailedEventReplayApprovalStatus.NOT_REQUESTED;
```

服务层重放门禁：
```java
if (!failedMessage.isReplayApproved()) {
    throw new ResponseStatusException(HttpStatus.CONFLICT, "failed event replay must be approved before replay");
}
```

页面前置门禁：
```javascript
if ((state.activeEvent?.replayApprovalStatus || "NOT_REQUESTED") !== "APPROVED") {
    showToast("重放前必须先审批通过", true);
    return;
}
```

审批接口：
```text
POST /api/v1/failed-events/{id}/replay-approval
POST /api/v1/failed-events/{id}/replay-approval/review
POST /api/v1/failed-events/{id}/replay
```

## 已执行验证

```powershell
node --check src/main/resources/static/failed-events.js
mvn "-Dtest=FailedEventSearchIntegrationTests,FailedEventManagementPageTests" test
mvn test
mvn -DskipTests package
```

结果：
```text
针对性测试：Tests run: 8, Failures: 0, Errors: 0, Skipped: 0
全量测试：Tests run: 30, Failures: 0, Errors: 0, Skipped: 0
打包：BUILD SUCCESS
```

全量测试里 Docker 可用，Testcontainers PostgreSQL 和 RabbitMQ 都实际启动并通过。

## HTTP 冒烟

本地打包后启动：
```powershell
java -jar target\advanced-order-platform-0.1.0-SNAPSHOT.jar `
  --server.port=18126 `
  --order.expiration.enabled=false `
  --outbox.publisher.enabled=false
```

冒烟结果：
```text
/failed-events.html -> HTTP 200，并包含 replayApprovalStatusFilter
/api/v1/failed-events?replayApprovalStatus=PENDING&page=0&size=5&sort=replayApprovalRequestedAt,desc
 -> page=0, size=5, totalElements=0, sort=replayApprovalRequestedAt,desc
/actuator/health/liveness -> UP
/actuator/health/readiness -> UP
```

说明：本次根 `/actuator/health` 返回 `DOWN` 且只暴露 `{"status":"DOWN","groups":["liveness","readiness"]}`，但 liveness/readiness、页面和 API 均正常。后续可以单独检查 Spring Boot health group 的根聚合策略。

## 图片记录

```text
a/25/图片/01-version-25-change-summary.png
a/25/图片/02-maven-test-v25.png
a/25/图片/03-package-http-smoke-v25.png
a/25/图片/04-replay-approval-flow-v25.png
a/25/图片/05-cleanup-v25.png
```

## 本版结论

v25 已经把失败事件重放从“操作员确认后可提交”升级为“申请、审批、通过后才能重放”。这让失败事件模块更接近真实生产运维系统：高风险动作可以做，但必须先留下审批意图和审批结果。
