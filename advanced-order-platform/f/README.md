# Java screenshot explanation archive f

This root is the canonical home for new Java runtime screenshots and screenshot explanations after the historical `d/` archive and the transitional `d_runtime_screenshot_archive_next/` handoff.

Do not place screenshots or explanation markdown directly in this root.

Use a version-range segment first, then a version folder:

```text
f/v<start>-v<end>/<version>/images/*.png
f/v<start>-v<end>/<version>/explanations/summary.md
```

Historical records stay where they were created:

```text
d/: historical runtime screenshots and explanations through v152
d_runtime_screenshot_archive_next/v1759-v1763/: transitional segmentation handoff
```

## Active Segments

```text
v1764-v1768: canonical f-root policy correction and archive handoff
v1769-v1785: reserved next f segment when screenshot evidence resumes
```
