# Java walkthroughs v153-v189

This folder contains 37 walkthroughs for the Java shard-readiness transition from the v153 echo through the v189 historical snapshot compatibility work.

Keep files in this folder when the explained Java version is between v153 and v189. Do not add generic notes here; use versioned filenames with the `version-<java-version>-<scope>.md` token so archive tests and readers can find the walkthrough.

Required writing structure is defined in `../README.md` and `../../代码讲解记录_写作规范.md`.
