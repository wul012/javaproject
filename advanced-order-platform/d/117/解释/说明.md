# Java v117 运行说明：v116 readiness echo 测试拆分

## 本版目标

本次暂时不读新计划，专注处理上版留下的“总行数多”问题。优先拆两个测试大文件：

```text
OpsEvidenceServiceTests.java
OpsOverviewIntegrationTests.java
```

v116 的 approval-required implementation readiness echo 行为不变；本版只调整测试组织结构。

## 拆分结果

新增：

```text
src/test/java/com/codexdemo/orderplatform/ops/OpsEvidenceServiceTestFixtures.java
src/test/java/com/codexdemo/orderplatform/ops/OpsEvidenceServiceApprovalRequiredImplementationReadinessEchoTests.java
src/test/java/com/codexdemo/orderplatform/OpsReleaseApprovalCredentialResolverReadinessIntegrationTests.java
```

迁出内容：

```text
v116 receipt 的单元契约测试
v116 /api/v1/ops/release-approval-rehearsal JSON 暴露测试
readOnlyFixtureService / headerBackedRehearsalRequest / paddedHeaderBackedRehearsalRequest 公共 fixture
```

## 行数变化

```text
OpsEvidenceServiceTests.java: 6758 -> 6367 行（-391）
OpsOverviewIntegrationTests.java: 5290 -> 5185 行（-105）
OpsEvidenceServiceApprovalRequiredImplementationReadinessEchoTests.java: 231 行
OpsReleaseApprovalCredentialResolverReadinessIntegrationTests.java: 128 行
OpsEvidenceServiceTestFixtures.java: 179 行
```

这次拆分不是为了改行为，而是把 v116 专项断言从两个大聚合测试类里移出来，让后续继续拆测试时有明确模式。

## 验证记录

已运行：

```text
mvn -q "-Dtest=OpsEvidenceServiceTests,OpsEvidenceServiceApprovalRequiredImplementationReadinessEchoTests,OpsOverviewIntegrationTests,OpsReleaseApprovalCredentialResolverReadinessIntegrationTests" test
mvn -q test
```

结果：

```text
聚焦测试通过
全量 Maven test 通过
v116 readiness echo receipt 行为保持
v116 API JSON 暴露保持
```

全量测试期间 Testcontainers 仍打印 Docker 环境不可用日志，但命令退出码为 0；本版不需要启动 Docker Desktop。

## 清理记录

Maven 测试生成的 `target/` 最终交付前删除。没有启动长期 Java 服务，没有启动 Docker Desktop，没有遗留后台进程。

## 一句话总结

Java v117 先从测试大文件开刀，把 v116 readiness echo 的单元和 MVC 测试拆成独立类，并抽出公共 fixture；行为不变，但两个最大测试文件分别减少 391 行和 105 行。
