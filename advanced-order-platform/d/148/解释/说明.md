# Java v148 runtime note

## Scope

Java v148 is a quality-only split. It does not add a new Node echo receipt and does not change runtime behavior.

The sandbox connection response record family moved out of `ReleaseApprovalRehearsalResponseRecords.java` into `ReleaseApprovalRehearsalSandboxConnectionResponseRecords.java`.

## Result

```text
ReleaseApprovalRehearsalResponseRecords.java: 624 lines
ReleaseApprovalRehearsalSandboxConnectionResponseRecords.java: 616 lines
OpsEvidenceService.java: 1037 lines
```

The split keeps the response model easier to maintain while preserving all existing response fields and constructor contracts.

## Verification

```powershell
mvn -q -DskipTests compile
mvn -q -DskipTests test-compile
mvn -q "-Dtest=OpsEvidenceServiceSandboxConnectionPacketEchoTests" test
mvn -q test
```

Result: passed.

## Screenshot

- `../图片/mcp-v148-sandbox-connection-response-records-split.png`
