# Java v139 运行说明

## 本次范围

Java v139 是质量优化版。根据 Node 当前计划，Java 本次不抢跑 Node v303，也不新增 runtime shell 链路；只把 v135/v136 runtime-shell echo support 中重复的只读边界骨架抽到 `ReleaseApprovalSandboxEndpointCredentialResolverRuntimeShellEchoBoundaryCatalog`。

## 验证命令

```powershell
mvn -q -DskipTests test-compile
mvn -q "-Dtest=ReleaseApprovalSandboxEndpointCredentialResolverRuntimeShellEchoBoundaryCatalogTests,OpsEvidenceServiceCredentialResolverRuntimeShellDecisionRecordEchoTests,OpsEvidenceServiceCredentialResolverRuntimeShellPostDecisionPlanIntakeEchoTests" test
mvn -q "-Dtest=ReleaseApprovalVerificationWarningDigestLineCatalogTests,ReleaseApprovalVerificationHintContributionCatalogTests,OpsEvidenceServiceReleaseApprovalRehearsalVerificationHintOverviewTests" test
```

测试只出现 Mockito/JDK 动态 agent 的常规 warning，没有失败。

本次没有启动 Docker Desktop，没有启动 Java/Node/mini-kv 后台服务。

## 截图

- `../图片/mcp-v139-runtime-shell-echo-boundary-catalog-quality.png`

截图由 Playwright MCP 生成，用于归档本版质量优化重点：runtime-shell decision record echo 与 post-decision plan intake echo 共享只读 side-effect boundary、warning digest boundary input、boundary line 和无凭证/无外联/无写入/无自启动证明逻辑。

## 结论

本版只改变代码组织，不改变 rehearsal schema、digest 语义或 runtime 行为。focused tests 锁住 v135/v136 echo 的边界顺序和值，overview 回归继续守住 verification hint 与 warning digest 接入链。
