# Java v118 运行说明：release approval 证据测试继续拆分

## 本版目标

本次不改生产逻辑，只继续拆分 `OpsOverviewIntegrationTests` 中最重的一段 release approval credential resolver rehearsal 场景。

## 拆分结果

新增：

```text
src/test/java/com/codexdemo/orderplatform/OpsReleaseApprovalCredentialResolverEndpointDecisionIntegrationTests.java
src/test/java/com/codexdemo/orderplatform/OpsReleaseApprovalCredentialResolverEndpointArchiveIntegrationTests.java
src/test/java/com/codexdemo/orderplatform/OpsReleaseApprovalCredentialResolverEndpointCandidateIntegrationTests.java
```

迁出内容：

```text
release approval credential resolver decision / disabled precheck / test-only shell / fake shell archive / production readiness blocked decision / pre-implementation plan intake / disabled implementation candidate
```

## 行数变化

```text
OpsOverviewIntegrationTests.java: 5185 -> 4336 行（-849）
OpsReleaseApprovalCredentialResolverEndpointDecisionIntegrationTests.java: 399 行
OpsReleaseApprovalCredentialResolverEndpointArchiveIntegrationTests.java: 245 行
OpsReleaseApprovalCredentialResolverEndpointCandidateIntegrationTests.java: 274 行
```

## 验证

已通过：

```text
mvn -q "-Dtest=OpsOverviewIntegrationTests,OpsReleaseApprovalCredentialResolverEndpointDecisionIntegrationTests,OpsReleaseApprovalCredentialResolverEndpointArchiveIntegrationTests,OpsReleaseApprovalCredentialResolverEndpointCandidateIntegrationTests,OpsReleaseApprovalCredentialResolverReadinessIntegrationTests" test
mvn -q test
```

## 清理

未启动 Docker Desktop；`mvn test` 期间 Testcontainers 只打印了 Docker 不可用日志，但命令退出码为 0。
