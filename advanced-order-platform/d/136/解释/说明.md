# Java v136 运行说明

## 本次范围

Java v136 按 Node `plans2/v300-post-runtime-shell-decision-record-upstream-echo-roadmap.md` 推进，只做 Node v301 post-decision plan intake 的只读 echo。

本次没有启动 Docker，也没有启动 Java/Node/mini-kv 后台服务。验证以 Maven 编译和 focused 单测为主，因为本版只改 release approval rehearsal 的内存 evidence/receipt 构造与断言，不需要数据库容器或浏览器访问真实页面。

## 验证命令

```powershell
mvn -q -DskipTests test-compile
mvn -q "-Dtest=OpsEvidenceServiceCredentialResolverRuntimeShellPostDecisionPlanIntakeEchoTests" test
mvn -q "-Dtest=OpsEvidenceServiceReleaseApprovalRehearsalVerificationHintOverviewTests" test
mvn -q "-Dtest=OpsEvidenceServiceCredentialResolverRuntimeShellDecisionRecordEchoTests,OpsEvidenceServiceReleaseApprovalRehearsalVerificationHintOverviewTests" test
mvn -q test
```

`mvn -q test` 退出码为 0。运行期间 Testcontainers 探测到本机 Docker 不可用并输出提示日志，但本次构建没有失败，也没有必要启动 Docker Desktop。

## 截图

- `../图片/mcp-v136-plan-intake-echo.png`

截图是 Playwright MCP 生成的 v136 evidence card，用于归档本版验证重点：Java v136 消费 Node v301、只读 echo、保持 runtime/credential/network/write/auto-start 边界关闭，并交给 Node v302 做后续 upstream echo verification。

## 结论

v136 receipt 已接入 response、verification hint、warning digest、proof claims 和 Node verification actions。focused 验证与全量 Maven 测试均通过；本版不授权 runtime shell 实现、调用、credential 读取、外部请求、ledger/SQL/schema 写入或自动启动。
