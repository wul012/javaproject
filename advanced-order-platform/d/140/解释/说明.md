# Java v140 运行说明

## 本次范围

Java v140 是质量优化版。根据 Node 当前计划，Java 本次仍不抢跑 Node v303，也不新增 runtime shell 链路；只把 v135/v136 runtime-shell echo support 中剩余的 metadata 清单抽到 `ReleaseApprovalSandboxEndpointCredentialResolverRuntimeShellEchoMetadataCatalog`。

## 验证命令

```powershell
mvn -q -DskipTests test-compile
mvn -q "-Dtest=ReleaseApprovalSandboxEndpointCredentialResolverRuntimeShellEchoMetadataCatalogTests,ReleaseApprovalSandboxEndpointCredentialResolverRuntimeShellEchoBoundaryCatalogTests,OpsEvidenceServiceCredentialResolverRuntimeShellDecisionRecordEchoTests,OpsEvidenceServiceCredentialResolverRuntimeShellPostDecisionPlanIntakeEchoTests" test
mvn -q "-Dtest=ReleaseApprovalVerificationWarningDigestLineCatalogTests,ReleaseApprovalVerificationHintContributionCatalogTests,OpsEvidenceServiceReleaseApprovalRehearsalVerificationHintOverviewTests" test
mvn -q test
```

测试只出现 Mockito/JDK 动态 agent 的常规 warning；全量测试里 Testcontainers 探测到 Docker 不可用但未导致失败。

本次没有启动 Docker Desktop，没有启动 Java/Node/mini-kv 后台服务。

## 截图

- `../图片/mcp-v140-runtime-shell-echo-metadata-catalog-quality.png`

截图由 Playwright MCP 生成，用于归档本版质量优化重点：v135/v136 runtime-shell echo 的 required evidence、no-go condition、continuation option、proof claim、Node warning/recommendation/action 等 metadata 已从 support 文件抽到独立 catalog。

## 结论

本版只改变代码组织，不改变 rehearsal schema、digest 语义或 runtime 行为。v139/v140 连续两版后，两个 runtime-shell support 从 575/582 行降到 418/399 行，后续 echo 扩展点更集中。
