# Java v137 运行说明

## 本次范围

Java v137 是独立质量优化版。根据 Node 当前计划，Node v302 走 Node 侧 echo segment catalog 质量优化；Java 本次不抢跑 Node v303，也不新增业务 echo，只做 `ReleaseApprovalVerificationHintBuilder` 的 contribution catalog 拆分。

## 验证命令

```powershell
mvn -q -DskipTests test-compile
mvn -q "-Dtest=ReleaseApprovalVerificationHintContributionCatalogTests" test
mvn -q "-Dtest=OpsEvidenceServiceReleaseApprovalRehearsalVerificationHintOverviewTests" test
```

本次没有启动 Docker Desktop，没有启动 Java/Node/mini-kv 后台服务。

## 截图

- `../图片/mcp-v137-verification-hint-catalog-quality.png`

截图由 Playwright MCP 生成，用于归档本版质量优化重点：`ReleaseApprovalVerificationHintBuilder` 不再内联 25 个 contribution 注册，注册表迁移到 `ReleaseApprovalVerificationHintContributionCatalog`，并由 focused test 守住数量、顺序和 v136 末尾 contribution。

## 结论

本版只改变代码组织，不改变 release approval rehearsal 的业务语义。verification hint 的 warning digest inputs、proof claims、node verification actions 仍由既有 overview 测试覆盖。
