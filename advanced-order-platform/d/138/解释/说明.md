# Java v138 运行说明

## 本次范围

Java v138 是质量优化版。根据 Node 当前计划，Node v303 仍需等待 Java v136、mini-kv v133 与 Node v302 完成后再推进；Java 本次不抢跑后续链路，只拆 `ReleaseApprovalVerificationWarningDigestBuilder` 的 warning/boundary line 注册清单。

## 验证命令

```powershell
mvn -q -DskipTests test-compile
mvn -q "-Dtest=ReleaseApprovalVerificationWarningDigestLineCatalogTests,OpsEvidenceServiceReleaseApprovalRehearsalVerificationHintOverviewTests,OpsEvidenceServiceReleaseApprovalRehearsalHeaderBackedOverviewTests" test
```

本次没有启动 Docker Desktop，没有启动 Java/Node/mini-kv 后台服务。

## 截图

- `../图片/mcp-v138-warning-digest-line-catalog-quality.png`

截图由 Playwright MCP 生成，用于归档本版质量优化重点：`ReleaseApprovalVerificationWarningDigestBuilder` 不再内联 25 个 warning lines 和 25 个 boundary lines 收集清单，改由 `ReleaseApprovalVerificationWarningDigestLineCatalog` 管理。

## 结论

本版只改变代码组织，不改变 warning digest 语义。focused catalog 测试守住 line 顺序和 v136 末尾条目，overview/header-backed 测试继续守住 warning digest inputs 与 digest 稳定性。
