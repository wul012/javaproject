# Java v212 v211 consumer handoff bundle snapshot freeze

v212 将 v211 consumer handoff bundle 收敛到 `OpsShardReadinessV1ContractConsumerHandoffBundleSnapshot.v211Bundle()`。

这样后续如果 bundle 增加字段或扩展 evidence 链，不会改写 v211 的 receipt、requiredEvidence、handoffEvidence 或 evidencePath。

本版没有新增 endpoint，也没有打开执行边界。
