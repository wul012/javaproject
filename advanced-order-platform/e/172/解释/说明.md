# Java v172 lifecycle plan route group split 说明

本版继续做维护性拆分，不新增 evidence gate，不改变 API path 或响应结构。

## 做了什么

- 新增 `OpsShardReadinessLifecyclePlanController`。
- 从 `OpsShardReadinessController` 拆出 active-shard plan、live-read gate plan、operator lifecycle、declared operator lifecycle 4 个 endpoint。
- 保持 `/api/v1/ops/shard-readiness/...` 路径不变。
- 对齐 Node v411-v412 的 route group split 思路，继续压低中央控制器复杂度。

## 明确没有做什么

- 没有新增 runtime approval 或 evidence gate。
- 没有启动或停止 Java / mini-kv。
- 没有连接 managed audit。
- 没有读取 credential 或 raw endpoint。
- 没有打开写路径或 active shard 路由。

## 验证口径

- 编译必须通过。
- lifecycle/plan 相关集成测试必须继续命中原路径。
- 全量 `mvn -q test` 必须通过。
- 本目录保留 JSON、HTML、截图和浏览器快照证据。
