# Java v206 shard readiness v1 contract endpoint pair integrity

- Adds `OpsShardReadinessV1ContractEndpointPairIntegrityTests`.
- Guards the extracted v1 contract endpoint group at exactly six pairs.
- Confirms read-only catalog and runtime execution endpoints do not drift into the v1 contract group.
- Confirms the v1 group remains positioned after shard-readiness echo and before the read-only evidence catalog in the rolling registry.
- Keeps behavior unchanged: no new route, no registry order change, and no execution boundary change.
