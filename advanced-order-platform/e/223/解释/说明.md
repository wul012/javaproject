# Java v223 v1 contract consumer evidence digest integrity

v223 adds `OpsShardReadinessV1ContractConsumerEvidenceDigestIntegrityTests`.

The test compares the frozen v220 digest with the frozen v215 checklist and the focused v1 endpoint-pair registry. It verifies digest checklist references, counts, blocked operations, digest evidence, and digest checks.

The guard also blocks digest self/future evidence from entering the frozen v220 digest.
