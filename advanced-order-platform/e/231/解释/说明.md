# Java v231 v1 contract consumer readiness handoff ops evidence alignment

v231 验证 readiness handoff 是否正确进入全局 OpsEvidence endpoint/probe 清单。

v229 已经证明 route 和 v1 endpoint-pair registry 对齐。v231 再往外走一层，确认共享的
`OpsShardReadinessEvidenceEndpoints` 和 `OpsEvidenceServiceTests` 也能看到 readiness handoff。

本版验证：

- live endpoint inventory 包含 readiness handoff；
- fixture endpoint inventory 包含 readiness handoff fixture；
- live probe 和 fixture probe 都包含 `GET` 形式的 handoff；
- checklist -> digest -> readiness handoff -> read-only catalog 的相对顺序不变；
- `OpsEvidenceServiceTests` 仍然通过。

这能防止后续只维护 v1 registry，却忘记同步 OpsEvidence 的 probe surface。
