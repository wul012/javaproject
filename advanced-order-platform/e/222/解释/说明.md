# Java v222 v220 consumer evidence digest historical compatibility

v222 adds `OpsShardReadinessV1ContractConsumerEvidenceDigestHistoricalCompatibilityTests`.

The test confirms that v220 digest endpoints are not backfilled into the older v179/v184 endpoint snapshots, and that the current rolling registry keeps the digest after the checklist and before read-only catalog endpoints.

It also keeps the frozen v220 `digestEvidence` list independent from v221 and v222 self/future receipts.
