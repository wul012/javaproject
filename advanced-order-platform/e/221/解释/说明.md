# Java v221 v220 consumer evidence digest snapshot freeze

v221 adds `OpsShardReadinessV1ContractConsumerEvidenceDigestSnapshot`.

The digest service now delegates to `v220Digest()`, preserving the v220 response as a stable historical baseline. The snapshot test checks the v215 checklist reference, digest evidence list, digest checks, and v220 evidence path.
