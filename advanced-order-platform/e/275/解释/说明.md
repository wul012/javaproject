# Java v275 readiness handoff validation artifact depth

v275 keeps the post-handoff catalog auditable by requiring each evidence JSON to carry both Maven test validation and
Playwright browser evidence. This keeps future receipts from becoming metadata-only markers.
