# Java v187 shard-readiness.v1 contract alignment

本版新增只读 contract alignment receipt，用于证明 Java 现有 root shard readiness 输出满足 plans3 中冻结的 `shard-readiness.v1` 最小字段。

- 新增 `/api/v1/ops/shard-readiness/v1-contract-alignment`。
- 新增静态 fixture `/contracts/java-shard-readiness-v1-contract-alignment-v187.fixture.json`。
- root `/api/v1/ops/shard-readiness` 仍保持 Java v153 schema，不做破坏性变更。
- endpoint registry 从 23 pair 增长到 24 pair。
- 本版没有打开 write routing、active shard router、credential 读取、raw endpoint parse、managed audit connection、deployment / rollback，也没有允许 Node 启停 Java 或 mini-kv。
