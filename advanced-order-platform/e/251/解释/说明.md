# Java v251 readiness handoff process-control boundary

v251 验证 readiness handoff 不涉及进程控制。

测试确认：
- `startsJavaService` 是 false；
- `startsMiniKvService` 是 false；
- `nodeMayStartOrStopJavaOrMiniKv` 是 false；
- blocked operations 和 handoff checks 都继续暴露这条禁止规则。
