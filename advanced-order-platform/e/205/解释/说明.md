# Java v205 shard readiness v1 contract endpoint pair split

- Adds `OpsShardReadinessV1ContractEndpointPairs` for the six v1 contract live/fixture endpoint pairs.
- Refactors `OpsShardReadinessEvidenceEndpoints` to compose that dedicated v1 list inside the existing rolling evidence order.
- Adds focused coverage for the extracted v1 endpoint pair order.
- Keeps behavior unchanged: no new route, no registry order change, and no execution boundary change.
