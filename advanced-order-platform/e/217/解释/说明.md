# Java v217 v215 consumer verification checklist historical compatibility

v217 adds `OpsShardReadinessV1ContractConsumerVerificationChecklistHistoricalCompatibilityTests`.

The test keeps the v215 checklist out of the older v179 and v184 endpoint snapshots. It also checks that the current rolling registry places the checklist after the v211 bundle and before read-only catalog endpoints.

The second boundary is evidence stability: the frozen v215 checklist does not include v216 snapshot-freeze evidence or this v217 historical-compatibility evidence in its own `requiredEvidence`.
