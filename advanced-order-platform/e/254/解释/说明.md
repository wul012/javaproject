# Java v254 readiness handoff read-only adjacency

v254 继续区分两条链：
- rolling evidence registry 可以从 readiness handoff 继续到 read-only catalog；
- frozen v1 consumer registry 仍然只到 readiness handoff，不包含 read-only catalog。

这防止 read-only catalog 被误认为 v1 consumer 合同的一部分。
