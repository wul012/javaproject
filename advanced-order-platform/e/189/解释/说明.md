# Java v189 v187 historical snapshot compatibility

本版是纯测试/证据守护。

- 当前 rolling endpoint registry 必须包含 v187 contract alignment live endpoint 和 fixture。
- v179、v184 历史冻结快照仍不反向包含 v187 endpoint，避免旧 receipt 被污染。
- v187 alignment snapshot 仍指向 root shard readiness，并固定 10 个最小字段。
- 本版没有新增生产代码、没有新增路由、没有打开任何执行能力。
