# Java v266 readiness handoff HTML archive version alignment

v266 checks that every HTML archive displays the same Java version as its catalog receipt.  This keeps visual review pages
aligned with the JSON evidence path and prevents version drift in archived handoff material.
