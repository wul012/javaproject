# Java v218 v1 contract consumer verification checklist integrity

v218 adds `OpsShardReadinessV1ContractConsumerVerificationChecklistIntegrityTests`.

The guard compares three layers:

- `OpsShardReadinessV1ContractEndpointPairs` keeps nine focused v1 endpoint pairs and excludes read-only/runtime execution groups.
- The v215 checklist keeps the v211 handoff bundle endpoint, fixture, evidence path, receipt, catalog count, and blocked operations.
- The v215 checklist required evidence references predecessor catalog/bundle receipts and excludes checklist self/future receipts.

This is an integrity-only version. It does not add a new endpoint or runtime execution capability.
