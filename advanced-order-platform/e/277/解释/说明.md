# Java v277 readiness handoff walkthrough filename alignment

v277 keeps code walkthrough archives easy to navigate by checking that filenames include both the receipt version token and
the scope slug. This makes the growing evidence set scan-friendly without changing runtime behavior.
