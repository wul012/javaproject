# Java v163 说明：runtime execution packet contribution

Node v396 已完成 Java v162 + mini-kv v153 runtime execution artifact upstream progress intake，并把下一步硬交接明确为六项 runtime execution packet requirement。Java v162 只是 Java-side candidate，不是跨项目 approval。Java v163 因此补上 Java-side runtime execution packet contribution：逐项回答 Node v396 的六行要求，但仍不形成跨项目可执行 packet。

## 输出

- Live endpoint: `GET /api/v1/ops/shard-readiness/runtime-execution-packet-contribution`
- Static fixture: `GET /contracts/java-shard-readiness-runtime-execution-packet-contribution-v163.fixture.json`
- Archive evidence: `e/163/evidence/java-shard-readiness-runtime-execution-packet-contribution-v163.json`
- Browser snapshot: `e/163/evidence/java-shard-readiness-runtime-execution-packet-contribution-v163-browser-snapshot.md`
- Screenshot: `e/163/图片/java-shard-readiness-runtime-execution-packet-contribution-v163.png`

## 本版 evidence

- `sourceRuntimeArtifactCandidateVersion=Java v162`
- `lastClarifiedByNodeVersion=Node v396`
- `nextNodeConsumerHint=Node v397`
- `javaPacketContributionPresent=true`
- `javaPacketContributionComplete=true`
- `crossProjectRuntimeExecutionPacketPresent=false`
- `crossProjectRuntimeExecutionPacketExecutable=false`
- `readyForRuntimeExecutionPacket=false`
- `readyForRuntimeLiveReadGate=false`
- `executionAllowed=false`
- `startsJavaService=false`
- `startsMiniKvService=false`

## 对 Node v396 六项要求的 Java 侧回答

- Operator approval record: Java 侧记录 `java-runtime-packet-contribution-approval-record-v163`，仍需 Node 关联跨项目 runtime window。
- Concrete loopback ports: Java 侧提供 `8080`，mini-kv 仍需提供自己的 packet contribution。
- GET-only smoke command: Java 侧提供三个 GET-only smoke command。
- Cleanup proof: Java 侧提供 cleanup owner 与 cleanup proof reference，不执行 cleanup。
- Service owner confirmation: Java 侧确认 `java-platform-operator-confirmed`。
- Process cleanup rules: Java 侧声明 stop-only-owned-process rules，禁止停止 pre-existing Java service。

## 仍缺的跨项目 artifacts

- `mini-kv-runtime-execution-packet-contribution`
- `node-approved-cross-project-runtime-window`
- `correlated-operator-approval-record-for-java-and-mini-kv`

## 边界

- 不修改 Java v153-v162 输出。
- 不启动或停止 Java / mini-kv。
- 不执行 runtime probe。
- 不读取 credential value 或 raw endpoint value。
- 不启用 active shard router、write routing、多进程分片或写命令。
- 不把 Java-only contribution 当成 cross-project runtime execution packet。

## 验证

```text
mvn -q -DskipTests compile
mvn -q "-Dtest=OpsShardReadinessRuntimeExecutionPacketContributionServiceTests,OpsShardReadinessRuntimeExecutionPacketContributionIntegrationTests,OpsShardReadinessRuntimeExecutionArtifactCandidateServiceTests,OpsShardReadinessRuntimeExecutionArtifactCandidateIntegrationTests,OpsShardReadinessEvidenceEndpointsTests,OpsEvidenceServiceTests" test
mvn -q test
```

定向测试和全量 `mvn -q test` 均已通过；测试期间仍会出现既有 Testcontainers Docker 探测日志和 Mockito/JDK 动态 agent 警告，但 Maven 退出码为 0。
