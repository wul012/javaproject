# Java v192 v190 handoff historical snapshot compatibility

- 仅修改测试，不改生产代码。
- 当前 rolling registry 必须包含 v190 handoff 的 live/fixture endpoint。
- v179 和 v184 历史快照不得被 v190 handoff 回填。
- 继续保持只读边界，不启用任何执行、写路由、凭据读取、managed audit、部署或回滚能力。
