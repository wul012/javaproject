# Java v257 readiness handoff validation command coverage

v257 检查每个 catalog evidence JSON 的 `validation` 字段。

要求每个版本都记录：
- Maven 测试命令；
- Playwright browser snapshot/screenshot 证据。

这让 evidence 不只是“通过”，还说明了如何验证。
