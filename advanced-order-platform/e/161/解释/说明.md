# Java v161 说明：declared operator lifecycle evidence

Node v387 已完成 Node v386 对 Java v160 与 mini-kv v151 operator service lifecycle evidence intake 的归档验证。v387 之后 Node 会等待 declared operator-owned lifecycle evidence。Java v161 在 Java 侧把 v160 的 placeholder 生命周期证据推进为 declared evidence：声明 owner、启动命令引用、端口、GET-only smoke、fail-closed 和 cleanup 责任。

## 输出

- Live endpoint: `GET /api/v1/ops/shard-readiness/declared-operator-lifecycle`
- Static fixture: `GET /contracts/java-shard-readiness-declared-operator-lifecycle-v161.fixture.json`
- Archive evidence: `e/161/evidence/java-shard-readiness-declared-operator-lifecycle-v161.json`
- Browser snapshot: `e/161/evidence/java-shard-readiness-declared-operator-lifecycle-v161-browser-snapshot.md`
- Screenshot: `e/161/图片/java-shard-readiness-declared-operator-lifecycle-v161.png`

## 本版 evidence

- `sourceLifecycleEvidenceVersion=Java v160`
- `lastVerifiedByNodeVersion=Node v387`
- `nextNodeConsumerHint=Node v388`
- `operatorLifecycleDeclared=true`
- `startupCommandDeclared=true`
- `portDeclared=true`
- `getOnlySmokeDeclared=true`
- `cleanupDeclared=true`
- `failClosedDeclared=true`
- `runtimeProbeAllowed=false`
- `nodeMayStartService=false`
- `nodeMayStopService=false`

## 生命周期声明

- Java service owner / start owner / stop owner 均声明为 `java-platform-operator`。
- Java working directory 声明为 `advanced-order-platform`。
- Java startup command 声明为 `mvn spring-boot:run -Dspring-boot.run.profiles=local`，只供 operator 执行参考，Node 不能从本证据执行。
- Java declared port 为 `8080`。
- Java base URL 只记录 handle：`java-local-readonly-base-url`。
- Smoke targets 全部为 GET-only。
- runtime live-read 仍需要 mini-kv declared lifecycle evidence 和单独批准的 runtime gate。

## 边界

- 不修改 Java v153-v160 输出。
- 不启动或停止 Java / mini-kv。
- 不执行 runtime probe。
- 不读取 credential value 或 raw endpoint value。
- 不启用 active shard router、write routing、多进程分片或写命令。

## 验证

```text
mvn -q -DskipTests compile
mvn -q "-Dtest=OpsShardReadinessDeclaredOperatorLifecycleServiceTests,OpsShardReadinessDeclaredOperatorLifecycleIntegrationTests,OpsShardReadinessOperatorServiceLifecycleServiceTests,OpsShardReadinessOperatorServiceLifecycleIntegrationTests,OpsShardReadinessEvidenceEndpointsTests,OpsEvidenceServiceTests" test
mvn -q test
```

定向测试和全量 `mvn -q test` 均已通过；测试期间仍会出现既有 Testcontainers Docker 探测日志和 Mockito/JDK 动态 agent 警告，但 Maven 退出码为 0。
