# Java v280 readiness handoff frozen boundary flags

v280 hardens the frozen v225 handoff by testing every dangerous operation flag directly. The catalog can grow, but the
frozen handoff remains read-only and non-executable.
