# Java v167 说明：runtime execution approval input template compatibility intake

Node v403 只读消费了 Node v402 template validator、Java v166 compatibility receipt 和 mini-kv v157 echo。Java v167 因此补一份 Java 侧 intake receipt：确认 Java 已把 Node v403 的只读 intake 边界纳入证据链，但仍不创建 `e/398/input/*` canonical approval input，不把模板或 intake 当成 runtime approval。

## 证据入口

- Live endpoint: `GET /api/v1/ops/shard-readiness/runtime-execution-approval-input-template-compatibility-intake`
- Static fixture: `GET /contracts/java-shard-readiness-runtime-execution-approval-input-template-compatibility-intake-v167.fixture.json`
- Archive evidence: `e/167/evidence/java-shard-readiness-runtime-execution-approval-input-template-compatibility-intake-v167.json`
- Browser snapshot: `e/167/evidence/java-shard-readiness-runtime-execution-approval-input-template-compatibility-intake-v167-browser-snapshot.md`
- Screenshot: `e/167/图片/java-shard-readiness-runtime-execution-approval-input-template-compatibility-intake-v167.png`

## 关键结论

- `sourceTemplateCompatibilityVersion=Java v166`
- `sourceNodeTemplateValidatorVersion=Node v402`
- `nodeCompatibilityIntakeVersion=Node v403`
- `miniKvTemplateEchoVersion=mini-kv v157`
- `nextNodeConsumerHint=Node v404`
- `compatibilityIntakeReceiptPresent=true`
- `compatibilityIntakeReceiptComplete=true`
- `readyForRuntimeExecutionPacket=false`
- `readyForRuntimeLiveReadGate=false`
- `executionAllowed=false`
- `status=passed`

## 仍然缺失的 canonical approval input

- `e/398/input/node-approved-runtime-window-v398.json`
- `e/398/input/correlated-operator-approval-record-v398.json`
- `e/398/input/cross-project-runtime-execution-packet-v398.json`

## v167 不做什么

- 不复制 Node v402 template 到 `e/398/input/*`。
- 不生成或伪造 `approvalCorrelationId`。
- 不把 Node v403 intake 当成 runtime approval。
- 不启动或停止 Java / mini-kv。
- 不执行 runtime probe。
- 不打开 managed audit connection。
- 不读取 credential value 或 raw endpoint value。
- 不启用 active shard router、write routing、多进程分片或写命令。

## 验证

定向测试和全量 `mvn -q test` 均已通过；测试期间仍会出现既有 Testcontainers Docker 探测日志、Mockito/JDK 动态 agent 警告，以及静态页面 favicon 404，但 Maven 退出码为 0。
