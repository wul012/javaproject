# Java v286 readiness handoff archive artifact byte floor

v286 adds a byte-floor check for recent evidence archives. It looks for versioned JSON, HTML, browser snapshot, and PNG
artifacts under each v275-v286 directory and fails if any of them are empty placeholders.
