# Java v162 说明：runtime execution artifact candidate

Node v395 已完成 Node v394 runtime execution artifact intake preflight 的归档验证。v394 记录当时没有 Java v162 / mini-kv v153 runtime artifact candidate，六项 runtime execution artifacts 仍为 0/6。Java v162 因此补上 Java 侧 runtime execution artifact candidate：声明 Java owner、loopback port、GET-only smoke command、cleanup proof reference 和 process cleanup rules。

## 输出

- Live endpoint: `GET /api/v1/ops/shard-readiness/runtime-execution-artifact-candidate`
- Static fixture: `GET /contracts/java-shard-readiness-runtime-execution-artifact-candidate-v162.fixture.json`
- Archive evidence: `e/162/evidence/java-shard-readiness-runtime-execution-artifact-candidate-v162.json`
- Browser snapshot: `e/162/evidence/java-shard-readiness-runtime-execution-artifact-candidate-v162-browser-snapshot.md`
- Screenshot: `e/162/图片/java-shard-readiness-runtime-execution-artifact-candidate-v162.png`

## 本版 evidence

- `sourceDeclaredLifecycleVersion=Java v161`
- `lastVerifiedByNodeVersion=Node v395`
- `nextNodeConsumerHint=Node v396`
- `javaRuntimeArtifactCandidatePresent=true`
- `javaRuntimeArtifactsComplete=true`
- `crossProjectRuntimeArtifactsComplete=false`
- `runtimeExecutionPacketPresent=false`
- `runtimeExecutionPacketExecutable=false`
- `readyForRuntimeExecutionPacket=false`
- `executionAllowed=false`
- `startsJavaService=false`
- `startsMiniKvService=false`

## Java 侧 artifact candidate

- Java service owner / startup command owner / cleanup owner 均声明为 `java-platform-operator`。
- Java working directory 声明为 `advanced-order-platform`。
- Java startup command 声明为 `mvn spring-boot:run -Dspring-boot.run.profiles=local`，但本证据不允许 Node 执行。
- Java loopback port 声明为 `8080`。
- Smoke commands 全部为 GET-only，并使用 `java-loopback-port-8080` handle。
- Cleanup proof 只记录 Java operator cleanup responsibility；没有执行 cleanup。
- Process cleanup rules 禁止停止 pre-existing Java service。

## 仍缺的跨项目 artifacts

- `mini-kv-v153-runtime-artifact-candidate`
- `cross-project-runtime-execution-packet`
- `node-approved-runtime-execution-window`

## 边界

- 不修改 Java v153-v161 输出。
- 不启动或停止 Java / mini-kv。
- 不执行 runtime probe。
- 不读取 credential value 或 raw endpoint value。
- 不启用 active shard router、write routing、多进程分片或写命令。
- 不把 Java v162 candidate 当成跨项目 runtime approval。

## 验证

```text
mvn -q -DskipTests compile
mvn -q "-Dtest=OpsShardReadinessRuntimeExecutionArtifactCandidateServiceTests,OpsShardReadinessRuntimeExecutionArtifactCandidateIntegrationTests,OpsShardReadinessDeclaredOperatorLifecycleServiceTests,OpsShardReadinessDeclaredOperatorLifecycleIntegrationTests,OpsShardReadinessEvidenceEndpointsTests,OpsEvidenceServiceTests" test
mvn -q test
```

定向测试和全量 `mvn -q test` 均已通过；测试期间仍会出现既有 Testcontainers Docker 探测日志和 Mockito/JDK 动态 agent 警告，但 Maven 退出码为 0。
