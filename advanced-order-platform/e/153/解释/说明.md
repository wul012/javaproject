# Java v153 说明：shard readiness echo

本版消费 Node v369 冻结的 `shard-readiness.v1`，只做 Java 侧只读 echo。

## 输出

- Live endpoint: `GET /api/v1/ops/shard-readiness`
- Static fixture: `GET /contracts/java-shard-readiness-v153.fixture.json`
- Archive evidence: `e/153/evidence/java-shard-readiness-v153.json`

## 契约字段

```text
project=advanced-order-platform
version=Java v153
readOnly=true
executionAllowed=false
shardEnabled=false
shardCount=0
slotCount=0
routingMode=fixture
evidencePath=e/153/evidence/java-shard-readiness-v153.json
status=passed
```

## 边界

- 不改订单事务。
- 不改支付或库存路由。
- 不写 ledger / SQL。
- 不启动 Node 或 mini-kv。
- 不连接 managed audit。

## 验证

```text
mvn -q -DskipTests compile
mvn -q -DskipTests test-compile
mvn -q "-Dtest=OpsShardReadinessServiceTests,OpsShardReadinessIntegrationTests,OpsEvidenceServiceTests" test
mvn -q test
```

全量测试通过；测试期间仍会出现既有 Testcontainers Docker 探测警告，但 Maven 退出码为 0。
