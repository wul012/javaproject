# Java v186 historical endpoint snapshot compatibility

本版新增纯测试守护，确认当前 rolling endpoint registry 仍覆盖 v179、v184 两个历史冻结快照。

- `OpsShardReadinessHistoricalEndpointSnapshotCompatibilityTests` 校验 rolling live / fixture registry 覆盖历史快照。
- v184 snapshot 必须覆盖 v179 snapshot。
- v179 snapshot 继续明确排除 v184 integrity endpoint，避免旧 receipt 被后续版本反向污染。
- 本版没有新增路由，没有修改生产代码，也没有打开任何执行能力。
