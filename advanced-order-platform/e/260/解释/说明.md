# Java v260 readiness handoff evidence scope summary

v260 检查 catalog 中每个 evidence JSON 的 `scope` 和 `summary`。

这版要求两者都明确落在 readiness handoff 语境里，避免后续归档只剩文件和 receiptId，但缺少可读的语义说明。

本版仍然只读，不新增 endpoint，也不改变 frozen v225 handoff。
