# Java v270 readiness handoff v1 endpoint registry stability

v270 protects the endpoint registry shape around the readiness handoff.  The v1 contract list stays at eleven pairs with
consumer readiness handoff last, while later read-only catalog and runtime closeout endpoints remain outside the list.
