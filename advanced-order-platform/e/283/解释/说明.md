# Java v283 readiness handoff catalog boundary schema strictness

v283 makes the catalog boundary object strict. Each evidence JSON must have exactly the expected read-only fields, and all
of those fields must remain false.
