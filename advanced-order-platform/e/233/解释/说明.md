# Java v233 v1 contract consumer readiness handoff fixture parity

v233 验证 readiness handoff 的 static fixture 与 frozen v225 response 完全一致。

这版用 Jackson 读取 `static/contracts/java-shard-readiness-v1-contract-consumer-readiness-handoff-v225.fixture.json`，
再把 `OpsShardReadinessV1ContractConsumerReadinessHandoffSnapshot.v225Handoff()` 转成 JSON tree，要求两棵 JSON tree 相等。

这比只检查几个字段更严格：字段名、布尔值、列表内容、列表顺序和字符串值都必须一致。

本版还补充验证 fixture endpoint 和 evidence path 与 service 常量一致，并带上 integration test，确保 live endpoint 和 fixture endpoint 仍能被 MockMvc 访问。
