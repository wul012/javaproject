# Java v168 说明：runtime execution approval input value validation

Node v405 已验证三份真实 canonical approval input：它们存在、值完整、共享同一个 `approvalCorrelationId`，并且只允许本地 loopback 的 GET-only smoke。Java v168 只做 Java 侧接收回执：承认 Node v405 的 value validation 已经把 runtime packet 推到 live-read gate 前置状态，但 Java 本版仍不启动服务、不执行 smoke、不读取 secret/raw endpoint、不写入任何状态。

## 证据入口

- Live endpoint: `GET /api/v1/ops/shard-readiness/runtime-execution-approval-input-value-validation`
- Static fixture: `GET /contracts/java-shard-readiness-runtime-execution-approval-input-value-validation-v168.fixture.json`
- Archive evidence: `e/168/evidence/java-shard-readiness-runtime-execution-approval-input-value-validation-v168.json`
- Browser snapshot: `e/168/evidence/java-shard-readiness-runtime-execution-approval-input-value-validation-v168-browser-snapshot.md`
- Screenshot: `e/168/图片/java-shard-readiness-runtime-execution-approval-input-value-validation-v168.png`

## 关键结论

- `sourceCompatibilityIntakeVersion=Java v167`
- `sourceNodePrecheckVersion=Node v404`
- `nodeValueValidationVersion=Node v405`
- `nextNodeConsumerHint=Node v406`
- `canonicalApprovalInputsPresent=true`
- `canonicalApprovalInputsValueValid=true`
- `sharedApprovalCorrelationIdValidated=true`
- `readyForRuntimeExecutionPacket=true`
- `readyForRuntimeLiveReadGate=true`
- `executionAttempted=false`
- `executionAllowed=false`
- `status=passed`

## 边界

- v168 不生成 `approvalCorrelationId`，只接收 Node v405 的验证结果。
- v168 不启动 Java / mini-kv。
- v168 不执行 runtime smoke。
- v168 不打开 managed audit connection。
- v168 不读取 credential value 或 raw endpoint value。
- v168 不启用 active shard router、write routing、多进程分片或写命令。

## 验证

定向测试和全量 `mvn -q test` 均已通过；测试期间仍会出现既有 Testcontainers Docker 探测日志、Mockito/JDK 动态 agent 警告，以及静态页面 favicon 404，但 Maven 退出码为 0。
