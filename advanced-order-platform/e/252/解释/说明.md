# Java v252 readiness handoff write/router boundary

v252 验证 readiness handoff 没有打开写路由或 active shard router。

测试确认：
- `writeRoutingAllowed` 是 false；
- `activeShardRouterAllowed` 是 false；
- `shardEnabled` 是 false；
- blocked operations 中仍然包含 `write-routing` 和 `active-shard-router`。
