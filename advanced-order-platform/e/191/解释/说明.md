# Java v191 v190 handoff snapshot freeze

- 新增 `OpsShardReadinessV1ContractAlignmentHandoffSnapshot`，冻结 v190 handoff 输入。
- `OpsShardReadinessV1ContractAlignmentHandoffService` 不再动态读取 alignment service 或当前 registry 来组装历史 receipt。
- 没有新增 route；v190 endpoint 和 fixture 保持不变。
- 继续保持只读边界：不开放写路由、不启用 active shard router、不读取凭据值、不连接 managed audit、不执行部署/回滚。
