# Java v184 read-only endpoint registry integrity

本版新增一个只读 endpoint registry integrity receipt，用于检查当前 shard-readiness endpoint pair registry 是否数量一致、无重复，并确认新增 endpoint 没有打开执行能力。

## 变更

- 新增 `OpsShardReadinessReadOnlyEndpointRegistryIntegrityService` 和 response。
- `OpsShardReadinessReadOnlyEvidenceController` 增加 `GET /api/v1/ops/shard-readiness/read-only-endpoint-registry-integrity`。
- `OpsShardReadinessEvidenceEndpoints` 新增 v184 live/fixture pair，当前 registry 变为 23 live / 23 fixture / 23 pair。
- 新增静态 fixture：`/contracts/java-shard-readiness-read-only-endpoint-registry-integrity-v184.fixture.json`。
- v179 verification snapshot 仍保持 22/22，不包含 v184 endpoint。

## 边界

- 只读 evidence endpoint，不做 write routing、active shard router、credential value、raw endpoint parse、managed audit connection、deployment / rollback。
- Node 仍不得自动启动或停止 Java / mini-kv。

## 验证

- focused: `mvn -q "-Dtest=OpsShardReadinessReadOnlyEndpointRegistryIntegrityServiceTests,OpsShardReadinessReadOnlyEndpointRegistryIntegrityIntegrationTests,OpsShardReadinessEvidenceEndpointsTests,OpsShardReadinessRoutePathsTests,OpsShardReadinessReadOnlyEvidenceControllerSplitTests,OpsShardReadinessReadOnlyEvidenceCatalogHandoffVerificationSnapshotTests,OpsEvidenceServiceTests" test`
- full: `mvn -q test`
