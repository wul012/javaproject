# Java v245 v1 contract consumer readiness handoff README index

v245 检查 `advanced-order-platform/e/README.md` 是否跟 post-handoff catalog 保持一致。

v243 已经能证明每个 catalog receipt 的文件存在。
但是如果 README 总索引漏掉某个版本，后续人工查证时仍然会断链。

本版测试读取 README，并对 catalog 中每个 receipt 检查：
- README 中存在 `- `<version>/`:` 形式的索引；
- README 中的版本顺序和 catalog 顺序一致；
- v245 自身的 evidence path 落在 `e/245/evidence/...v245.json`。

这让证据归档从“文件存在”推进到“索引可发现”。
