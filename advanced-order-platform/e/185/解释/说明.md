# Java v185 v184 registry integrity snapshot freeze

本版把 v184 的 endpoint registry integrity receipt 改为读取冻结快照。

- `OpsShardReadinessReadOnlyEndpointRegistryIntegritySnapshot` 固定 v184 当时的 23 pair / 23 live / 23 fixture。
- `OpsShardReadinessReadOnlyEndpointRegistryIntegrityService` 不再读取滚动 registry 生成 v184 receipt。
- 后续继续新增只读 endpoint 时，v184 的历史计数不会被动漂移。
- 本版没有新增执行入口，没有打开 write routing、active shard router、credential 读取、raw endpoint parse、managed audit connection、deployment / rollback，也没有允许 Node 启停 Java 或 mini-kv。
