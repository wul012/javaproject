# Java v250 readiness handoff audit/deployment boundary

v250 验证 managed audit connection 与 deployment/rollback 没有被 readiness handoff 打开。

这两类能力都属于执行或运维动作，不属于当前只读交接范围。
测试确认：
- `managedAuditConnectionAllowed` 是 false；
- `deploymentOrRollbackAllowed` 是 false；
- blocked operations 中仍然包含对应条目。
