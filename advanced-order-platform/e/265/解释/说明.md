# Java v265 readiness handoff screenshot artifact completeness

v265 verifies screenshot artifacts as real PNG files rather than placeholder paths.  The check keeps visual evidence useful
for review while preserving the read-only handoff boundary.
