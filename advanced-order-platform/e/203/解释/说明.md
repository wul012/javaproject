# Java v203 shard readiness v202 consumer probe plan snapshot freeze

- Freezes the Java v202 consumer probe plan behind `OpsShardReadinessV1ContractConsumerProbePlanSnapshot`.
- Switches the probe plan service to return the frozen v202 snapshot instead of rebuilding from the current manifest service.
- Adds snapshot coverage so read targets, fixture targets, probe sequence, required evidence, stop conditions, and verification checks remain stable.
- Keeps the boundary closed: no write routing, active shard router, credential value reads, raw endpoint parsing, managed audit connection, deployment/rollback, Java/mini-kv process control, or upstream actions.
