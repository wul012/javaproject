# Production Excellence Progress 闁?Java

Source playbook: `D:\nodeproj\orderops-node\docs\plans\production-excellence-java-final-push.md`.

This file is the Java repo-local progress tracker. The Node playbook is
read-only for Java sessions; do not write progress back into `D:\nodeproj`.

## Deviations

- The playbook's 2026-06-12 starting fact said no CI existed. Current Java repo
  already had root `.github/workflows/maven-ci.yml`; J0 updates that workflow
  instead of creating a duplicate file.
- `target/` is ignored and is not tracked by git.
- The first local wrapper command timed out; the successful rerun fixed Maven
  Wrapper distribution to Maven 3.9.9.
- J7 v1797 closeout was completed by Claude acting as executor (operating-model
  change: the user asked Claude to take over the three projects on 2026-06-14).
  Codex had staged the extraction but left `spotless:check` failing on one moved
  file; the executor ran `spotless:apply` to fix it, then a full `mvnw verify`
  passed all gates (tests + JaCoCo + SpotBugs + Spotless). J7 is a new milestone
  beyond the playbook's J0闁炽儲褰?: J6 produced the inventory/ratchet scaffolding;
  J7 is the first actual ops-class extraction batch.
- Historical tag-quality note: v1798's tag remains a historical failed-CI tag,
  and v1799 records/remediates the walkthrough gate issue. Do not rewrite or
  force-move historical tags; treat v1799 and newer tags as the remediated line,
  and v1805 as the current green baseline until a newer Java tag passes CI.

## Extraction checkpoint v1848-v1852 (Claude PASS)

- Five dependency-safe batches moved 102 non-controller files: direct root
  `573 -> 471`, remaining movable `468 -> 366`, retained root `105`, unassigned `0`.
- Operator-CI, ArchiveVerificationHandoff, RuntimeExecution, and ReadOnlyEvidence
  buckets are all zero; total `ops` main Java remains `1,352`.
- All ten implementation/closeout CI runs passed: `29139989169`, `29140017745`,
  `29140642676`, `29140670166`, `29141739750`, `29141793170`, `29142950353`,
  `29142974989`, `29144146898`, and `29144180677`.
- Claude independently passed the checkpoint with no corrections and re-ran the
  Node live capstone against Java HEAD `a7237a85`; v1853 resumes extraction.

## Progress

| Milestone | Version(s) | State | Evidence |
| --------- | ---------- | ----- | -------- |
| J68 | v1858 | local gates passed; remote CI pending | Moves the exact 18-file RouteCleanup maintenance core and nine owned behavior tests into `ops.maintenance.routecleanup`; the Spring controller and upkeep catalog remain root composition adapters. Nine byte-identical suffixes move to `RouteCleanupRoutes`, only nine measured immutable ENDPOINT fields become public, PROFILE stays package-private, and all 20 existing SpotBugs response mirrors follow their FQNs. Direct root 249 -> 231, movable 145 -> 127, RouteCleanup 141 -> 123, total `ops` main Java 1,352, unassigned 0. The 3,998-Han walkthrough preceded implementation. A new 60-line `OpsExtractionTestSupport` centralizes repeated filesystem and walkthrough mechanics, and v1857 now consumes it. `test-compile` passed after two narrow retained-test imports; focused gates and Spotless passed. Full `mvnw verify` passed 1,831 tests in 13m03s, JaCoCo checked 2,228 classes with all checks met, and SpotBugs analyzed 2,316 classes with 0 findings. Details: `docs/ops/route-cleanup-maintenance-core-extraction-v1858.md`. |
| J67 | v1857 | completed; remote CI passed | Moves the exact 29-file read-only-gate closure and 11 owned tests into `ops.maintenance.routecleanup`; direct root 278 -> 249, movable 174 -> 145, RouteCleanup 170 -> 141, total `ops` main Java 1,352, and unassigned 0. Ten route suffixes keep their bytes through `RouteCleanupRoutes`; the endpoint manifest merges both route owners while preserving legacy response names, and the public analyzer record defensively copies its list boundary. Focused gates passed 153 tests; final local `mvnw verify` passed 1,823 tests in 9m07s with JaCoCo 2,228 classes and SpotBugs 0; implementation commit `13c046f6` and GitHub Actions run `29173393345` passed. Details: `docs/ops/route-cleanup-read-only-gate-extraction-v1857.md`. |
| J66 | v1856 | completed; remote CI passed | Moves the complete 12-file Prototype implementation closure and three package behavior tests into `ops.maintenance.prototype`; three Spring controllers and three controller-split tests remain root-visible. A nested `PrototypeRoutes` owner takes all 30 byte-identical suffixes out of the global route aggregator, which shrinks from 1,111 to 1,058 lines. `CloseoutSource` plus a four-field immutable snapshot inverts the previous Prototype -> root RouteCleanup service/response dependency; the existing root closeout service is the adapter, and only catalog plus field-alignment endpoints are public for their two verified readers. Direct root 290 -> 278, movable 186 -> 174, both Prototype buckets -> 0, total `ops` main Java 1,352, unassigned 0. The first test compile exposed only a package-private root test fixture; its existing closeout factory became public in test source without widening production. The second test compile passed. Focused gates completed 26 Surefire reports with 95 tests, 0 failures, 0 errors, and 0 skipped in 6m37s. Full `mvnw verify` passed 1,816 tests in 8m43s; JaCoCo analyzed 2,229 classes with all checks met, and SpotBugs reported 0 findings. Production/test budgets tightened to 1058/34/1/1 and remained 853/8/2/0. The 4,655-Han Chinese-majority walkthrough preceded implementation. Implementation commit `586560cd`; GitHub Actions run `29159207263` passed. This ledger commit is the tagged closeout. Details: `docs/ops/prototype-closure-extraction-v1856.md`. |
| J65 | v1855 | completed; remote CI passed | Moves the complete 20-file readiness core and ten package behavior tests into `ops.maintenance.readinesscore`; three controllers, the global endpoint/route registries, and cross-family tests remain root-visible. The base readiness service owns the base route plus three evidence suffixes while the root aggregator delegates, removing the package-to-root route dependency without changing bytes. Direct root 310 -> 290, movable 206 -> 186, both readiness buckets -> 0, total `ops` main Java 1,352, unassigned 0. Initial focused failures were maintenance-only (one overbroad stale-import predicate plus two live census pins); a follow-up discoverability gate required the README index. Corrected focused selection passed 81 tests. First full verify ran 1,806 tests and exposed only eight historical live pins, one v1852 old path, and four walkthrough-standard checks; repairs preserve all thresholds and history. Final full verify passed all 1,806 tests in 8m35s, JaCoCo analyzed 2,228 classes with all checks met, and SpotBugs reported 0 findings. Production/test budgets remain 1111/34/1/1 and 853/8/2/0. Implementation commit `2f990c73`; GitHub Actions run `29157609557` passed. Details: `docs/ops/readiness-core-extraction-v1855.md`. |
| J64 | v1854 | completed; remote CI passed | Moves the complete 118-file direct-root ReleaseApproval closure plus the now-family-specific `ContextHeaderField` into `ops.maintenance.releaseapproval`; six package tests/support files follow it. Direct root 429 -> 310, movable 324 -> 206, all eight ReleaseApproval buckets 118 -> 0, shared-root waivers 4 -> 3, unassigned 0, and total `ops` main Java remains 1,352. The extracted family owns 89 Java contract constants and 227 Node upstream constants in bounded 400/800-line catalogs; `OpsEvidenceService` retains one public compatibility alias but the package has zero reverse service references. A forwarding-only execution-denied builder is folded into its matching support to offset the second catalog. The first 179-test focused run exposed only maintenance/path/document gates; two 1,340/995-line overview tests were split into seven semantic files without raising the 854/8/2/0 test-source budget. Final focused gates passed 184 tests. Full `mvnw verify` passed 1,799 tests in 13m22s, JaCoCo analyzed 2,228 classes with all checks met, and SpotBugs reported 0 findings. Implementation commit `d512a84b`; GitHub Actions run `29155544134` passed. Details: `docs/ops/release-approval-closure-extraction-v1854.md`. |
| J63 | v1853 | completed; remote CI passed | Extracts all 42 V1Contract implementation files and 98 package tests into `ops.maintenance.v1contract`; controller and three controller/route tests remain root. Direct root 471 -> 429, movable 366 -> 324, bucket 42 -> 0, unassigned 0; walkthrough has 3,341 Han. Focused gates passed 245 tests. First full run exposed only the unchanged 1,111-line RoutePaths budget; one blank line was removed without loosening it. Final verify passed 1,785 tests in 10m03s, JaCoCo 2,229 classes, SpotBugs 0. Implementation commit `411f695b`; GitHub Actions run `29151790238` passed. Details: `docs/ops/v1-contract-consumer-alignment-extraction-v1853.md`. |
| J62 | v1852 | completed; remote CI passed | Moves the 11-file ReadOnlyEvidence catalog/handoff/verification/integrity closure and seven package-local tests into `ops.maintenance.readonlyevidence`; the controller and ControllerSplit test remain root-visible. A public test-source TestSupport forwards six immutable v175/v179/v184 lists and centralizes four service factories while all three production snapshots stay package-private. The v184 snapshot replaces its implicit dependency on the root registry's private nested pair with its own package-private immutable pair. Direct root 482 -> 471, remaining 377 -> 366, ReadOnlyEvidence bucket 11 -> 0, unassigned 0; the 3,704-Han walkthrough preceded final verification. The first test compile exposed one stale factory method name; after call-site-only repair, 1,483 main and 850 test sources compiled. The first focused run passed 76/77 tests and exposed only an overstrict new guard requiring the root reflection test to import the internal package; the guard now checks its controller and four unchanged routes instead. Final focused gates passed with 77 tests. The first full run passed 1,777/1,778 tests and exposed only a one-line RoutePaths budget overage after formatting public constants; the 1,111 budget remained unchanged and one grouping blank line was removed. Final full verify passed in 10m10s with 1,778 tests, JaCoCo met, SpotBugs 0. Implementation commit `6b6d4216`; GitHub Actions runs `29144146898` and `29144180677` passed. Details: `docs/ops/read-only-evidence-catalog-extraction-v1852.md`. |
| J61 | v1851 | completed; remote CI passed | Moves the complete 18-file RuntimeExecution evidence chain and nine behavior tests into `ops.maintenance.runtimeexecution`; the Spring controller remains root-visible. A new test-source TestSupport becomes the sole full-chain graph builder and the retained root factory delegates to it. Existing endpoint, fixture, and evidence strings remain byte-identical and only become public immutable references for ReadOnlyEvidence and worksheet consumers. Direct root 500 -> 482, remaining 395 -> 377, RuntimeExecution buckets 18 -> 0, unassigned 0; the 4,837-Han walkthrough preceded final verification. `test-compile` passed for 1,483 main and 848 test sources; focused gates passed with 97 tests. The first full run passed 1,770/1,771 tests and exposed one stale v1847 live root-count pin at 500; its historical transition stayed unchanged while the live shrink-only pin tightened to 482. Final full verify passed in 9m29s with 1,771 tests, JaCoCo met, SpotBugs 0. Implementation commit `b83406ad`; GitHub Actions runs `29142950353` and `29142974989` passed. Details: `docs/ops/runtime-execution-evidence-extraction-v1851.md`. |
| J60 | v1850 | completed; remote CI passed | Moves 25 ArchiveVerificationHandoff implementation files and six package-local tests into `ops.maintenance.releasearchivehandoff`; the controller and controller Markdown test remain root-visible. Literal/chosen package paths measured 222/234 and 200/212. The handoff consumes only v1849 Archive and v1840 route boundaries; RoutePathSplit imports only the public service/response. Direct root 525 -> 500, remaining 420 -> 395, Handoff bucket 25 -> 0, unassigned 0; the 3,289-Han walkthrough preceded verification. Compiler and Chinese-majority gates exposed and closed stale-import/document-depth issues without changing behavior or thresholds; final focused gates passed with 75 tests. Full verify passed in 12m33s with 1766 tests, JaCoCo met, SpotBugs 0. Implementation commit `500dbe28`; GitHub Actions runs `29141739750` and `29141793170` passed. Details: `docs/ops/release-acceptance-archive-verification-handoff-extraction-v1850.md`. |
| J59 | v1849 | completed; remote CI passed | Moves 23 ReleaseAcceptanceArchive implementation files and six package-local tests into path-safe `ops.maintenance.ciarc`; the controller and controller Markdown test remain root-visible. Literal package paths measured 354/361 characters versus 252/259 for the chosen package. The layer consumes only v1848 ReleaseAcceptance and v1840 route boundaries; ArchiveVerificationHandoff imports only the public service/response. Direct root 548 -> 525, remaining 443 -> 420, Operator-CI 23 -> 0, unassigned 0; the 3,283-Han walkthrough preceded verification. Focused gates passed with 54 tests; full verify passed in 14m27s with 1760 tests, JaCoCo met, SpotBugs 0. Implementation commit `bc024ed5`; GitHub Actions runs `29140642676` and `29140670166` passed. Details: `docs/ops/operator-ci-release-acceptance-archive-extraction-v1849.md`. |
| J58 | v1848 | completed; remote CI passed | Moves 25 ReleaseAcceptance base implementation files and seven package-local tests into path-safe `ops.maintenance.ciaccept`; the controller and controller Markdown test remain root-visible. Literal package paths measured 339/347 characters versus 247/255 for the chosen package. The layer consumes only v1847 VerificationDossier and v1840 route boundaries; Archive imports only the public service/response. Direct root 573 -> 548, remaining 468 -> 443, Operator-CI 48 -> 23, unassigned 0; the 3,844-Han walkthrough preceded verification. Focused gates passed with 52 tests; the first full run exposed the v1828 live census, which was shrink-only reconciled while preserving history; final full verify passed in 11m42s with 1754 tests, JaCoCo met, SpotBugs 0. Implementation commit `4f8a8ed2`; GitHub Actions runs `29139989169` and `29140017745` passed. Details: `docs/ops/operator-ci-release-acceptance-extraction-v1848.md`. |
| J57 | v1847 | completed; remote CI passed | Moves 25 VerificationDossier implementation files and seven package-local tests into path-safe `ops.maintenance.operatorcidossier`; the controller and controller Markdown aggregate test remain root-visible. Literal/intermediate package paths measured 307/272 characters, while the chosen package keeps the longest moved test path at 248; no public name changed. The dossier consumes only the v1846 ConsumerPackage boundary and v1840 route owner; twelve ReleaseAcceptance producers import its public service/response. Direct root 598 -> 573, remaining 493 -> 468, Operator-CI bucket 73 -> 48, unassigned 0; the 3,168-Han walkthrough preceded verification. Focused gates passed in 52s; full verify passed in 8m18s with 1748 tests, JaCoCo met, SpotBugs 0. Implementation commit `0a344c1f`; GitHub Actions runs `29136629004` and `29136673279` passed. Details: `docs/ops/operator-ci-verification-dossier-extraction-v1847.md`. |
| J56 | v1846 | completed; remote CI passed | Moves 23 ConsumerPackage implementation files and four package-local tests into path-safe `ops.maintenance.minimalreadonlygateoperatorciconsumerpackage`; the controller and controller Markdown aggregate test remain root-visible. The literal full-prefix package measured 264 characters, while the chosen semantic package measures at most 244; no class, route, or response name changed. The package consumes only the v1845 digest boundary and v1840 route owner; twelve VerificationDossier producers import its public service/response. Direct root 621 -> 598, remaining 516 -> 493, Operator-CI bucket 96 -> 73, unassigned 0; the 3,379-Han walkthrough preceded verification. Focused gates passed in 1m09s; full verify passed in 10m30s with 1742 tests, JaCoCo met, SpotBugs 0. Implementation commit `6f0ddd98`; GitHub Actions runs `29136099277` and `29136157965` passed. Details: `docs/ops/minimal-read-only-gate-operator-ci-consumer-package-extraction-v1846.md`. |
| J55 | v1845 | completed; remote CI passed | Moves 17 ArchiveDigest implementation files and five package-local tests into `ops.maintenance.minimalreadonlygateoperatorcihandoffarchivedigest`; the controller and controller Markdown test remain root-visible. The package consumes only the v1844 archive-verification boundary and v1840 route owner; eleven ConsumerPackage producers now import the public digest service/response. Direct root 638 -> 621, remaining 533 -> 516, Operator-CI bucket 113 -> 96, unassigned 0; the 3,734-Han walkthrough preceded verification. Focused gates passed in 1m02s; full verify passed in 11m38s with 1736 tests, JaCoCo met, SpotBugs 0. Implementation commit `67cbdae6`; GitHub Actions runs `29135508710` and `29135560354` passed. Details: `docs/ops/minimal-read-only-gate-operator-ci-handoff-archive-digest-extraction-v1845.md`. |
| J54 | v1844 | completed; remote CI passed | Moves 27 Operator-CI base/archive-verification implementation files and seven package-local tests into `ops.maintenance.minimalreadonlygateoperatorcihandoff`; two controllers and two controller Markdown tests remain root-visible. The package consumes the v1843 execution boundary and v1840 route owner, while ArchiveDigest imports only its public archive-verification service/response. Direct root 665 -> 638, remaining 560 -> 533, Operator-CI bucket 140 -> 113, unassigned 0; the 4,297-Han walkthrough preceded verification. First focused run exposed the v1843 historical guard's old root path; its assertion was preserved and repointed to the new package, then focused gates passed. Full verify passed in 13m19s with 1730 tests, JaCoCo met, SpotBugs 0. Implementation commit `1edff88f`; GitHub Actions runs `29134858842` and `29134919829` passed. Details: `docs/ops/minimal-read-only-gate-operator-ci-handoff-core-extraction-v1844.md`. |
| J53 | v1843 | completed; remote CI passed | Moves all 31 MinimalReadOnlyGateExecution non-controller implementation files plus 13 package-local tests into `ops.maintenance.minimalreadonlygateexecution`; two Spring controllers and two controller tests remain root-visible. Operator-CI consumers now import the narrow archive-verification boundary, all eight SpotBugs response mirrors follow the moved FQNs, and route strings remain owned by the existing v1840 public route owner. Direct root 696 -> 665, remaining 591 -> 560, Execution bucket 31 -> 0, unassigned 0; the 4,136-Han Chinese walkthrough preceded verification. Focused gates passed; full verify passed in 13m10s with 1724 tests, JaCoCo met, SpotBugs 0. Implementation commit `3ec5b24c`; GitHub Actions runs `29133921177` and `29133997449` passed. Details: `docs/ops/minimal-read-only-gate-execution-extraction-v1843.md`. |
| J52 | v1842 | completed; remote CI passed | Completes the three-version ReleaseAcceptanceRoutePathSplit track by moving 36 acceptance-package, closeout-receipt, and closeout-archive-index implementation files plus eight package-local tests into `ops.maintenance.releaseacceptancepackage`; three Spring controllers and three controller-oriented tests remain root-visible. The closure consumes the v1841 sustainment boundary, preserves the v1840 route owner, exposes only three service/response boundaries and three test factories, and relocates all 10 SpotBugs response mirrors. Direct root 732 -> 696, remaining 627 -> 591, split bucket 36 -> 0, unassigned 0; the 5,277-Han Chinese walkthrough was written before verification. The initially deeper package path hit the Windows path limit before package rewrites, so all 36 files were reconciled into the shorter semantic package with exact-list proof. Focused gates passed; full verify passed in 16m01s with 1718 tests, JaCoCo met, SpotBugs 0. Implementation commit `7477a931`; GitHub Actions runs `29096822114` and `29096874655` passed. Details: `docs/ops/release-acceptance-package-extraction-v1842.md`. |
| J51 | v1841 | completed; remote CI passed | Moves the 19-file ReleaseAcceptanceRoutePathSplit sustainment implementation plus four package-local tests into `ops.maintenance.releaseacceptanceroutepathsplit.sustainment`; the Spring controller and its controller test remain root-visible. The moved layer consumes only the v1840 closeout boundary, exposes only its public service/response to the queued acceptance package, relocates all four SpotBugs response mirrors, and keeps every runtime/write/credential/deployment boundary closed. Direct root 751 -> 732, remaining 646 -> 627, split bucket 55 -> 36, unassigned 0; the 5,252-Han Chinese walkthrough was written before verification. Focused gates passed; full verify passed in 11m46s with 1712 tests, JaCoCo met, SpotBugs 0. Implementation commit `4daf785a`; GitHub Actions runs `29094941270` and `29094991888` passed. Details: `docs/ops/release-acceptance-route-path-split-sustainment-extraction-v1841.md`. |
| J50 | v1840 | completed; remote CI passed | Starts the ReleaseAcceptanceRoutePathSplit track by moving 17 base files, 6 closeout files, and the shared route owner into `ops.maintenance.releaseacceptanceroutepathsplit`; two controllers remain root-visible. Global/split owners compare through 11 narrow immutable constants; direct root 775 -> 751, remaining 670 -> 646, split bucket 78 -> 55, owner bucket 1 -> 0. Full verify passed in 14m01s with 1707 tests, JaCoCo met, SpotBugs 0. Implementation commit `74be83bc`; GitHub Actions runs `29093379485` and `29093448061` passed. Details: `docs/ops/release-acceptance-route-path-split-base-closeout-extraction-v1840.md`. |
| J49 | v1839 | completed; remote CI passed | Moves 14 EvaluationPreflight implementation files to `ops.maintenance.comparedevidenceevaluationpreflight`, keeps its controller root-visible, evolves the public EndpointRefs into the sole RoutePaths owner, and closes Review -> Evaluation -> Blueprint/ProfileSection imports. Direct root 789 -> 775, remaining 684 -> 670, unassigned 0; full verify passed in 10m22s with 1702 tests, JaCoCo met, SpotBugs 0. Implementation commit `e1c3ac99`; GitHub Actions runs `29091607033` and `29091658155` passed. Details: `docs/ops/compared-evidence-evaluation-preflight-extraction-v1839.md`. |
| J48 | v1838 | completed; remote CI passed | Dependency-safe ComparedPackageReview extraction moves 16 implementation files to `ops.maintenance.comparedpackagereview`, keeps the controller root-visible, replaces EndpointRefs with the sole public route owner, and lowers direct-root ops 805 -> 789 (remaining 700 -> 684, unassigned 0). First full verify exposed route-aggregator 1113 > 1111; the implementation was compressed without raising the cap, and final verify passed in 10m51s with 1697 tests, JaCoCo met, SpotBugs 0. Implementation commit `76bd1336`; GitHub Actions runs `29090265566` and `29090421968` passed. Details: `docs/ops/compared-package-review-extraction-v1838.md`. |
| J47 | v1837 | completed; remote CI passed | Replaces 34-builder and multi-dozen receipt parameter fan-out with one package-private verification context backed by the canonical `ReceiptChain`; extracts no-ledger-write evaluation without changing digest/proof/action order. Hint/digest/response files 874/675/564 -> 70/421/421; consolidated verification support is 412; aggregate >500/>750 counts 38/4 -> 35/3. First full verify exposed a +2 root-file census violation; context/support consolidation restored direct-root 805, remaining 700, unassigned 0 without loosening ratchets. Exact-list, header-backed, integration, all affected historical census, architecture, budget, walkthrough, Spotless, and SpotBugs gates passed; final full verify passed in 10m01s with 1692 tests, JaCoCo met and SpotBugs 0. Implementation commit `c3574002`; tag `v1837-order-platform-production-excellence-release-approval-verification-composition-split`; GitHub Actions run `29069744872` passed. Details: `docs/maintainability/release-approval-verification-composition-v1837.md`. |
| J46 | v1836 | completed; remote CI passed | Splits dead-letter recording, management, replay approval, and replay execution from the public transactional facade. Facade 662 -> 199 lines; new collaborators 71/89/129/222/33; aggregate >500 count 39 -> 38 while >750/>1000 stay at 4/2. Focused Spring, architecture, budget, walkthrough, Spotless, and SpotBugs gates passed; full verify passed in 9m35s with 1689 tests, JaCoCo met and SpotBugs 0. Implementation commit `446bddbe`; tag `v1836-order-platform-production-excellence-failed-event-command-responsibility-split`; GitHub Actions run `29066081716` passed. Details: `docs/maintainability/failed-event-command-split-v1836.md`. |
| J45 | v1835 | completed; remote CI passed | Splits failed-event query orchestration, JPA specifications, and page/sort validation from the public facade; facade 1126 -> 662 lines, new collaborators 310/159/103, aggregate >750/>1000 counts 5/3 -> 4/2. Focused gates passed with 17 tests; full verify passed in 10m58s with 1682 tests, JaCoCo met and SpotBugs 0. Implementation commit `db373a41`; tag `v1835-order-platform-production-excellence-failed-event-query-responsibility-split`; GitHub Actions run `29063754021` passed. Details: `docs/maintainability/failed-event-query-split-v1835.md`. |
| J44 | v1834 | completed; remote CI passed | Java hotspot census plus shrink-only aggregate and named-file budgets; baseline 1475 files, max 1530 lines, thresholds 39/5/3. Local full verify passed with 1676 tests; commit `7d935d3a`, tag `v1834-order-platform-production-excellence-java-maintainability-hotspot-budget`, and GitHub Actions run `29060123279` passed. Details: `docs/maintainability/java-maintainability-program-v1834.md`. |
| J43 | v1833 | completed; remote CI passed | Compared-evidence candidate-intake-preflight extraction lowered direct-root ops 819 -> 805 and remaining movable files 714 -> 700. Commit `df44a1cf`, tag `v1833-order-platform-production-excellence-ops-compared-evidence-candidate-intake-preflight-extraction`, and GitHub Actions run `28848977233` passed; details: `docs/ops/compared-evidence-candidate-intake-preflight-extraction-v1833.md`. |
| J42 | v1832 | completed; remote CI passed | Thirty-third contract-preserving ops extraction moves the `ComparedEvidenceCandidateBlueprint` implementation into `ops.maintenance.comparedevidencecandidateblueprint`, keeps the Spring controller in root, adds a public candidate-blueprint route owner, folds the old EndpointRefs helper into that route owner, updates CandidateIntakePreflight and ProfileSection endpoint readers to import the moved boundary, publicizes only the still-root `ComparedEvidenceEvaluationPreflightEndpointRefs` immutable constants needed by the moved section catalogs, relocates the SpotBugs candidate-blueprint response FQN, and updates the endgame census. Direct root files 833 -> 819; remaining movable root files 728 -> 714; total ops Java stays 1,352. `scripts/ops-root-census.ps1 -Json` reports 819 direct-root files, 105 retained-root files, 714 remaining direct-root non-controller files, 0 unassigned files, and a 0-count `ComparedEvidenceCandidateBlueprint` bucket. Focused candidate-blueprint, CandidateIntakePreflight, ProfileSection, readability, ratchet, and walkthrough gates passed with 74 tests; Spotless passed. Full `mvnw verify` passed in 11m02s with 1,661 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. Commit `df7ef777`, tag `v1832-order-platform-production-excellence-ops-compared-evidence-candidate-blueprint-extraction`, pushed to `javaproject/master`, and GitHub Actions run `28846590626` passed. |
| J41 | v1831 | completed; remote CI passed | Thirty-second contract-preserving ops extraction moves the `OperatorEvidenceValueSupply` base implementation into `ops.maintenance.operatorevidencevaluesupply`, keeps the two Spring controllers in root, adds a public ValueSupply route owner, updates AdapterPreflight and ApprovalPreflight endpoint readers to import the moved base services, folds the old Support helper into `SlotCatalog`, relocates the SpotBugs base response FQN, and updates the endgame census. Direct root files 848 -> 833; remaining movable root files 743 -> 728; total ops Java stays 1,352. `scripts/ops-root-census.ps1 -Json` reports 833 direct-root files, 105 retained-root files, 728 remaining direct-root non-controller files, 0 unassigned files, and 0-count `OperatorEvidenceValueSupplyAdapterPreflight` plus `OperatorEvidenceValueSupply base` buckets. Focused ValueSupply base, AdapterPreflight, ApprovalPreflight, readability, ratchet, and walkthrough gates passed with 114 tests; Spotless passed. Full `mvnw verify` passed in 12m11s with 1,650 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. Commit `dc838171`, tag `v1831-order-platform-production-excellence-ops-operator-evidence-value-supply-base-extraction`, pushed to `javaproject/master`, and GitHub Actions run `28844172949` passed. |
| J40 | v1830 | completed; remote CI passed | Thirty-first contract-preserving ops extraction moves the `OperatorEvidenceValueSupplyAdapterPreflight` implementation into `ops.maintenance.operatorevidencevaluesupplyadapterpreflight`, keeps the two Spring controllers in root, adds a public AdapterPreflight route owner, folds the old `RuleCatalog` into `SlotCatalog` to avoid net file growth, relocates the SpotBugs response FQN, and updates the endgame census. Direct root files 864 -> 848; remaining movable root files 759 -> 743; total ops Java stays 1,352. `scripts/ops-root-census.ps1 -Json` reports 848 direct-root files, 105 retained-root files, 743 remaining direct-root non-controller files, 0 unassigned files, and a 0-count `OperatorEvidenceValueSupplyAdapterPreflight` bucket. Focused AdapterPreflight/ApprovalPreflight/readability/walkthrough tests passed with 90 tests; v1829/v1830 historical guard focused tests passed with 18 tests; Spotless passed; full `mvnw verify` passed in 11m24s with 1,642 tests, JaCoCo coverage checks met, and SpotBugs reporting no bugs. Commit `5019e651`, tag `v1830-order-platform-production-excellence-ops-operator-evidence-value-supply-adapter-preflight-extraction`, pushed to `javaproject/master`, and GitHub Actions run `28841817323` passed. |
| J39 | v1829 | completed; remote CI passed | Thirtieth contract-preserving ops extraction closes the three-version ProfileSection cluster by moving ten `SignedApprovalDraftProfileSectionHandoff` implementation files into `ops.maintenance.signedapprovaldraftprofilesectionhandoff`. The handoff and registry Spring controllers remain in root. The handoff suffix is owned by `OpsShardReadinessSignedApprovalDraftProfileSectionRoutePaths`, with root and candidate-document route catalogs delegating to the same value. Direct root files 874 -> 864; remaining movable root files 769 -> 759; total ops Java stays 1,352. This batch also adds the reviewer census command `scripts/ops-root-census.ps1`, v1829 docs, readability guard coverage, SpotBugs FQN relocation, and Chinese walkthrough. `scripts/ops-root-census.ps1` reports 864 direct-root files, 105 retained-root files, 759 remaining direct-root non-controller files, and 0 unassigned files. Focused v1829/Handoff/walkthrough tests passed with 53 tests; Spotless passed; full `mvnw verify` passed in 13m43s with 1,632 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. Commit `edd72224`, tag `v1829-order-platform-production-excellence-ops-signed-approval-draft-profile-section-handoff-extraction`, pushed to `javaproject/master`, and GitHub Actions run `28833833554` passed. |
| J38 | v1828 | completed; remote CI passed | Java final-push Phase 1 endgame census version. It fixes the remaining direct-root `ops` package at 874 files, the final direct-root target at 105 files, and the remaining move/collapse backlog at 769 files. It adds `docs/ops/extraction-endgame-census-v1828.md`, starts `docs/ops/extraction-waivers.md`, and adds v1828 readability guard coverage. No route, response, write boundary, runtime profile, deployment, rollback, archive path, or root `ops` file count changes. Focused census/docs tests passed with 15 tests; Spotless passed; full `mvnw verify` passed in 13m40s with 1,624 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. Commit `b516be6d`, tag `v1828-order-platform-production-excellence-ops-extraction-endgame-census`, and GitHub Actions run `28795645950` passed. |
| J37 | v1827 | completed; remote CI passed | Java final-push Step 0 reconciliation closes the mismatch between the progress ledger and the actual v1826 state, then resolves the long-floating project explanation file. It records v1826 commit `dd3e1db0`, tag `v1826-order-platform-production-excellence-ops-signed-approval-draft-text-package-profile-section-extraction`, pushed `javaproject/master`, and GitHub Actions run `27874073004` as completed. It commits `docs/project-explanation/project-value-and-flow.md` as real explanatory content, adds `docs/project-explanation/README.md`, and adds `docs/ops/java-final-push-step0-reconciliation-v1827.md` plus documentation guard coverage. No route, response, write boundary, runtime profile, deployment, rollback, archive path, or root `ops` file count changes. Focused docs tests passed with 12 tests; Spotless passed; full `mvnw verify` passed in 19m11s with 1,621 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. Commit `66895274`, tag `v1827-order-platform-production-excellence-java-final-push-step0-reconciliation`, and GitHub Actions run `28791751229` passed. |
| J36 | v1826 | completed; remote CI passed | Twenty-ninth contract-preserving ops extraction continues the 36-file, three-version ProfileSection cluster by moving the text-package ProfileSection implementation into `ops.maintenance.signedapprovaldrafttextpackageprofilesection`. Twelve implementation files move, the standalone GateCatalog is folded into RegistrySupport, the controller stays root, and a public signed-approval route owner replaces the historical CandidateDocument-owned literal while preserving the byte-identical path through delegation. Direct root files 887 -> 874; total ops Java stays 1,352. The moved registry consumes six already-extracted text-package read-only sources plus three still-root compared-evidence candidate sources, with explicit imports making the mixed boundary visible. Thirteen package-local tests move with the implementation; the retained-root controller test imports the public test support. Focused tests passed with 53 tests after tightening the exact docs phrase and Chinese-majority walkthrough content; Spotless passed; full `mvnw verify` passed in 13m39s with 1,617 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. Commit `dd3e1db0`, tag `v1826-order-platform-production-excellence-ops-signed-approval-draft-text-package-profile-section-extraction`, and GitHub Actions run `27874073004` passed. |
| J35 | v1825 | completed; remote CI passed | Twenty-eighth contract-preserving ops extraction starts the correctly scoped 36-file, three-version ProfileSection cluster. It moves the nine surviving base implementation files into `ops.maintenance.signedapprovaldraftprofilesection`, folds the standalone GateCatalog into RegistrySupport, and leaves the controller in root. Direct root files 897 -> 887; total ops Java stays 1,352. A public route owner preserves the byte-identical registry path, and the historical CandidateDocument route constant delegates to the same owner. The public service/response boundary keeps the retained-root controller and future ProfileSectionHandoff extraction aligned. Thirteen package-local tests move with the implementation; the controller and Handoff test factories import the new boundary. Focused tests passed with 72 tests after aligning the explanation with the standard Chinese walkthrough sections; Spotless passed; full `mvnw verify` passed in 10m33s with 1,611 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. Commit `2d8d66f8`, tag `v1825-order-platform-production-excellence-ops-signed-approval-draft-profile-section-extraction`, and GitHub Actions run `27872866366` passed. |
| J34 | v1824 | completed; remote CI passed | Twenty-seventh contract-preserving ops extraction moves thirteen signed-approval artifact-draft-text-package-compared-package-evidence-intake implementation files into `ops.maintenance.signedapprovalartifactdrafttextpackagecomparedpackageevidenceintake`, folds GuardCatalog into SlotCatalog, and leaves the controller plus root aggregator in root. Direct root files 911 -> 897; total ops Java stays 1,352. A public route owner preserves five byte-identical paths. The moved catalogs consume the v1821 SubmissionPreflight and v1823 ComparisonAcceptancePrecheck boundaries; ComparedPackageReview and ProfileSection readers import the new public endpoint/response boundary. SpotBugs exclusions were relocated to the moved response FQN, and the legacy FQN is absent. Focused tests and Spotless passed; full `mvnw verify` passed in 10m46s with 1,605 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. Commit `2791a771`, tag `v1824-order-platform-production-excellence-ops-signed-approval-artifact-draft-text-package-compared-package-evidence-intake-extraction`, and GitHub Actions run `27871548217` passed. |
| J33 | v1823 | completed; remote CI passed | Twenty-sixth contract-preserving ops extraction moves seven signed-approval artifact-draft-text-package-comparison-acceptance-precheck implementation files into `ops.maintenance.signedapprovalartifactdrafttextpackagecomparisonacceptanceprecheck`, collocates GuardCatalog with CheckpointCatalog, and leaves the controller plus root aggregator in root. Direct root files 919 -> 911; total ops Java stays 1,352. A public route owner preserves four byte-identical paths. The moved checkpoint catalog consumes five v1822 ComparisonPreflight endpoints; ComparedPackageEvidenceIntake and ProfileSection import the public endpoint/response boundary. Main compile and test compile passed; Spotless passed; 55 focused tests passed, including the walkthrough compliance gate; full `mvnw verify` passed in 6m09s with 1,600 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. Commit `55b1256f`, tag `v1823-order-platform-production-excellence-ops-signed-approval-artifact-draft-text-package-comparison-acceptance-precheck-extraction`, and GitHub Actions run `27862508049` passed. |
| J32 | v1822 | completed; remote CI passed | Twenty-fifth contract-preserving ops extraction moves twelve signed-approval artifact-draft-text-package-comparison-preflight implementation files into `ops.maintenance.signedapprovalartifactdrafttextpackagecomparisonpreflight`, collocates GateCatalog with AcceptanceControlCatalog, and leaves the controller plus root aggregator in root. Direct root files 932 -> 919; total ops Java stays 1,352. A public route owner preserves five byte-identical paths. Moved lanes consume v1821 SubmissionPreflight endpoints; ComparisonAcceptancePrecheck, ComparedPackageEvidenceIntake, and ProfileSection import the new public boundary. Main compile and test compile passed; 60 focused tests passed after tightening one exact documentation phrase and Chinese-majority explanation; Spotless passed; full `mvnw verify` passed in 14m29s with 1,595 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. Commit `2b96e209`, tag `v1822-order-platform-production-excellence-ops-signed-approval-artifact-draft-text-package-comparison-preflight-extraction`, and GitHub Actions run `27859120258` passed. |
| J31 | v1821 | completed; remote CI passed | Twenty-fourth contract-preserving ops extraction moves the primary signed-approval artifact-draft-text-package-submission-preflight and its tightly coupled Closeout family into `ops.maintenance.signedapprovalartifactdrafttextpackagesubmissionpreflight`: 28 physical implementation files move, GateCatalog is collocated with ComparisonControlCatalog, and 2 controllers + root aggregator stay root. Direct root files 961 -> 932; total ops Java stays 1,352. A public route owner preserves five primary plus six Closeout HTTP paths; three Closeout detail services remain catalog fragments. ComparisonPreflight, ComparedPackageEvidenceIntake, and ProfileSection readers import the new public boundary. Main compile and test compile passed; 136 focused tests passed; Spotless passed; full `mvnw verify` passed in 12m36s with 1,590 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. Commit `95a209db`, tag `v1821-order-platform-production-excellence-ops-signed-approval-artifact-draft-text-package-submission-preflight-extraction`, and GitHub Actions run `27828087482` passed. |
| J30 | v1820 | completed; remote CI passed | Twenty-third contract-preserving ops extraction moves signed-approval artifact-draft-text-package-review-preflight into `ops.maintenance.signedapprovalartifactdrafttextpackagereviewpreflight`: 15 implementation files move, GateCatalog is collocated with RejectionControlCatalog, and 2 controllers + root aggregator stay root. Direct root files 977 -> 961; total ops Java stays 1,352. A public route owner preserves nine byte-identical paths. Moved criteria consume v1819 Intake endpoints; SubmissionPreflight and ProfileSection readers import the new public boundary. Added v1820 docs/tests, SpotBugs FQN relocation, and three count-ratchet updates. Focused tests passed after correcting one exact documentation guard phrase; Spotless passed; full `mvnw verify` passed in 7m31s with 1,585 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. GitHub Actions run `27823512964` succeeded. |
| J29 | v1819 | local gates passed; remote CI pending | Twenty-second contract-preserving ops extraction moves the operator-evidence-value-supply signed-approval artifact-draft-text-package-intake registry family into `ops.maintenance.signedapprovalartifactdrafttextpackageintake`: 15 physical implementation files move, the package-private gate catalog is collocated with the guard catalog to offset the new route owner, and 2 controllers + aggregator stay root. Direct root `ops` Java files 993 -> 977 while total `ops` Java files stay at 1,352. The new public `OpsShardReadinessSignedApprovalArtifactDraftTextPackageIntakeRoutePaths` owner holds the family suffix constants while the root aggregator delegates to it, preserving route strings. Cross-family ENDPOINT sub-recipe: the moved field catalogs read already-public v1818 `ArtifactDraftInstructionPreflight` endpoint constants, and retained-root `TextPackageReviewPreflight` plus `SignedApprovalDraftTextPackageProfileSection` readers import this family's public immutable endpoint strings. No route string, response shape, write boundary, credential boundary, deployment, rollback, or archive layout changes. Added `docs/ops/signed-approval-artifact-draft-text-package-intake-extraction-v1819.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1819Tests`; ratchets `MAX_ROOT_OPS_MAIN_JAVA_FILES`, `EXPECTED_ROOT_OPS_MAIN_JAVA_FILES`, and the exact measured root guard 993->977. Local focused tests passed with 186 tests, Spotless passed, and full `mvnw verify` passed in 9m03s with 1,580 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. |
| J0 | v1790 | completed; Claude reviewed PASS | `mvnw` generated for Maven 3.9.9; Docker tests tagged; Surefire excludes `docker` by default; `docker-tests` profile selects Docker-tagged tests; local wrapper verifies passed; GitHub Actions run `27397723739` succeeded; Claude reviewed J0 as PASS |
| J1 | v1791 | completed; remote CI passed | Maven Enforcer requires Java 21 and Maven 3.9.9; SpotBugs first scan found 2602 legacy findings collapsed into a shrink-only baseline; Spotless ratchet defaults to the Java canonical remote locally and CI overrides the baseline explicitly; temporary local formatting violation proved `spotless:check` fails; local Spotless, SpotBugs, focused tests, default verify, and docker profile verify passed; GitHub Actions run `27408969138` succeeded |
| J2 | v1792 | completed; remote CI passed | JaCoCo 0.8.15 added with `jacocoArgLine` + `test.jvm.argLine` split; baseline recorded in `docs/coverage/jacoco-baseline-v1792.md`; global line baseline 98.18% with floor 0.96; package floors cover root/catalog/common/inventory/notification/ops/readability/order/outbox/payment; outbox temporary floor 0.99 proved the check fails; docker profile sets `jacoco.skip=true` because it is not a coverage representative suite; GitHub Actions run `27412812905` succeeded and uploaded the JaCoCo report |
| J3 | v1793 | completed; remote CI passed | Added `application-prod.yml` with H2 console and SQL debug output disabled, graceful shutdown enabled, and health probes retained; CI prod profile boot smoke starts the packaged jar and checks `/actuator/health`; `compose.yaml` and `docker-compose.yml` now use env substitution plus `.env.example`; order and failed-event write request DTOs now have Bean Validation boundaries; focused prod/config/order/failed-event validation tests, walkthrough compliance, Spotless ratchet check, default verify, Java 21 prod smoke, docker profile verify, and GitHub Actions run `27415924092` passed |
| J4 | v1794 | completed; remote CI passed | Added Micrometer Tracing Brave bridge, trace/span logging pattern, explicit health/info/metrics exposure in default and prod profiles, request log correlation helper, ApiExceptionHandler trace/span logs, actuator info/metrics tests, tracing configuration tests, MDC fallback tests, and a real HTTP validation-error trace log test; focused J4 tests, walkthrough compliance, Spotless ratchet check, default verify, Java 21 prod smoke, docker profile verify, and GitHub Actions run `27418381630` passed |
| J5 | v1795 | completed; remote CI passed | Added CHANGELOG version policy, PRODUCTION_READINESS boundary centralization, README release discipline pointers, ProductionReadinessDocumentationTests, and v1795 Chinese walkthrough; focused docs tests, walkthrough compliance, Spotless ratchet check, default verify, Java 21 prod smoke, docker profile verify, and GitHub Actions run `27420759966` passed |
| J6 | v1789+, v1796 | completed; remote CI passed | v1789 added ops consolidation roadmap and ratchet; v1796 adds ops route-family inventory, load-bearing archive boundary, first reduction candidate list, docs pointer, inventory guard tests, and aligns the local Spotless ratchet default with `javaproject/master` before any class movement; local Spotless, focused tests, full verify, docker profile verify, prod smoke, and GitHub Actions run `27425536260` passed |
| J7 | v1797 | completed (Claude executor closeout 2026-06-14) | First contract-preserving ops extraction moves eleven code walkthrough compliance implementation files into `ops.maintenance.walkthrough.compliance`, lowers direct root `ops` Java files from 1,330 to 1,319, keeps the root controller and shared route aggregation stable, adds extraction docs/tests, and preserves archive boundaries. Executor closeout: `test-compile` clean; focused tests (`OpsShardReadinessCodeWalkthroughCompliance*`, `ReadabilityUpkeep*`, `ProductionReadinessDocumentationTests`) exit 0; **full `mvnw verify` BUILD SUCCESS in 514s 闁?JaCoCo "all coverage checks met" (per-package floors survived the move), SpotBugs check passed (no new findings), Spotless clean**. Codex had left one moved file (`OpsShardReadinessCodeWalkthroughComplianceRegistryResponse.java`) with a Spotless violation; fixed via `spotless:apply` before commit. Tagged `v1797-order-platform-production-excellence-ops-walkthrough-compliance-extraction`. |
| J8 | v1798 | completed (Claude executor closeout 2026-06-14) | Second contract-preserving ops extraction moves ten code walkthrough quality gate registry implementation files into `ops.maintenance.walkthrough.qualitygate`, lowering direct root `ops` Java files from 1,319 to 1,309, keeping the root controller + shared route aggregation stable. Followed the J7 recipe: made the family `OpsShardReadinessCodeWalkthroughQualityGateRoutePaths` public with its own `BASE_PATH` and repointed the moved service to it (route byte-identical); made `ENDPOINT` public; package-local service/renderer/boundary/immutability/test-support tests moved into the subpackage; root controller + route-path tests construct the service directly and import the public route-path class. Relocated the moved Response's accepted `EI_EXPOSE_REP/REP2` exclusions in `config/spotbugs-exclude.xml` to the new FQN (same accepted findings, not new 闁?shrink-only baseline preserved). Added `docs/ops/quality-gate-registry-extraction-v1798.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1798Tests`; lowered the ratchet `MAX_ROOT_OPS_MAIN_JAVA_FILES` 1319闁?309. Executor closeout: **full `mvnw verify` BUILD SUCCESS in 6m23s 闁?1492 tests 0 failures, JaCoCo "all coverage checks have been met" (per-package floors survived the move), SpotBugs check passed (16 EI_EXPOSE findings re-covered by the relocated exclusions, 0 new), Spotless clean**. Tagged `v1798-order-platform-production-excellence-ops-quality-gate-registry-extraction`. |
| J9 | v1799 | completed (Claude executor closeout 2026-06-14) | Third contract-preserving ops extraction moves eleven code walkthrough quality audit registry implementation files into `ops.maintenance.walkthrough.qualityaudit`, lowering direct root `ops` Java files from 1,309 to 1,298, keeping the root controller + shared route aggregation stable. Same recipe as J8: family `OpsShardReadinessCodeWalkthroughQualityAuditRoutePaths` made public with its own `BASE_PATH`, service repointed (route byte-identical), `ENDPOINT` public; package-local service/renderer/boundary/immutability/closeout/test-support tests moved into the subpackage; root controller + route-path tests construct the service directly. Relocated the moved Response's accepted `EI_EXPOSE_REP/REP2` exclusions to the new FQN (pre-applied, so SpotBugs passed first try). Added `docs/ops/quality-audit-registry-extraction-v1799.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1799Tests`; lowered the ratchet 1309闁?298. **Also fixed a latent gate failure inherited from v1798**: the v1798 Chinese walkthrough had been written after that version's verify and committed un-reverified, below the 3000-CJK / Chinese-majority threshold of `OpsCodeWalkthroughArchiveComplianceTests`; this run caught it and the v1798 walkthrough was expanded to pass. Executor closeout: **full `mvnw verify` BUILD SUCCESS in 7m12s 闁?1495 tests 0 failures, JaCoCo "all coverage checks have been met", SpotBugs check passed (0 new), Spotless clean**. Tagged `v1799-order-platform-production-excellence-ops-quality-audit-registry-extraction`. |
| J10 | v1800 | completed (Claude executor closeout 2026-06-14) | Fourth contract-preserving ops extraction moves eight code walkthrough depth registry implementation files into `ops.maintenance.walkthrough.depth`, lowering direct root `ops` Java files from 1,298 to 1,290, keeping the root controller + shared route aggregation stable. Completes moving all four CodeWalkthrough registry families out of root. Same recipe: family `OpsShardReadinessCodeWalkthroughDepthRoutePaths` made public with its own `BASE_PATH`, service repointed (route byte-identical), `ENDPOINT` public; package-local service/renderer/boundary/test-support tests moved into the subpackage; root controller + route-path tests construct the service directly. Relocated the moved Response's accepted `EI_EXPOSE_REP/REP2` exclusions to the new FQN (pre-applied). Walkthrough written before verify per the v1799 lesson. Added `docs/ops/depth-registry-extraction-v1800.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1800Tests`; lowered the ratchet 1298闁?290. Executor closeout: **full `mvnw verify` BUILD SUCCESS 闁?tests 0 failures, JaCoCo "all coverage checks have been met", SpotBugs check passed (0 new), Spotless clean**. Tagged `v1800-order-platform-production-excellence-ops-depth-registry-extraction`. |
| J11 | v1801 | completed (Claude executor closeout 2026-06-14) | Fifth contract-preserving ops extraction 闁?first outside the CodeWalkthrough family. Moves ten screenshot explanation archive registry implementation files into `ops.maintenance.screenshotexplanationarchive`, lowering direct root `ops` Java files from 1,290 to 1,280, keeping the root controller + shared route aggregation stable. Same recipe: family `OpsShardReadinessScreenshotExplanationArchiveRoutePaths` made public with its own `BASE_PATH`, service repointed (route byte-identical), `ENDPOINT` public; package-local service/renderer/boundary/immutability/closeout/f-root-policy/test-support tests moved into the subpackage; segmentation-docs/controller/route-path tests stay root (controller+route-path construct the service directly). Relocated the moved Response's accepted `EI_EXPOSE_REP/REP2` exclusions to the new FQN (pre-applied). Walkthrough written before verify. Added `docs/ops/screenshot-explanation-archive-extraction-v1801.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1801Tests`; lowered the ratchet 1290闁?280. Executor closeout: **full `mvnw verify` BUILD SUCCESS 闁?tests 0 failures, JaCoCo "all coverage checks have been met", SpotBugs check passed (0 new), Spotless clean**. Tagged `v1801-order-platform-production-excellence-ops-screenshot-explanation-archive-extraction`. |
| J12 | v1802 | completed (Claude executor closeout 2026-06-14) | Sixth contract-preserving ops extraction 闁?second family outside CodeWalkthrough and the FIRST dependency-injected "evidence" registry. Moves eleven credential resolver disabled fake harness evidence archive implementation files into `ops.maintenance.credentialresolver`, lowering direct root `ops` Java files from 1,280 to 1,269, keeping the root controller + shared route aggregation stable. Family `OpsShardReadinessCredentialResolverRoutePaths` made public (public BASE_PATH+suffix); root controller + aggregator import it. Because the service is DI-built with the shared `OpsEvidenceService` and tests build it via `OpsEvidenceServiceTestFixtures`, two behaviour-neutral visibility steps were needed: moved service/catalogs import the public `OpsEvidenceService`/`ReleaseApprovalRehearsalResponse`, and the shared `OpsEvidenceServiceTestFixtures` was made public so the moved test support reuses it. Package-local catalog test + test support moved to subpackage; the combined service/controller test stays in root and constructs the service directly via the shared fixture. Relocated the moved Response's accepted `EI_EXPOSE_REP/REP2` exclusions (6 entries incl. `$FakeHarnessBoundary`) to the new FQN. Walkthrough written before verify. Added `docs/ops/credential-resolver-disabled-fake-harness-evidence-archive-extraction-v1802.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1802Tests`; lowered the ratchet 1280闁?269. Executor closeout: **full `mvnw verify` BUILD SUCCESS 闁?tests 0 failures, JaCoCo "all coverage checks have been met", SpotBugs check passed (0 new), Spotless clean**. Tagged `v1802-order-platform-production-excellence-ops-credential-resolver-disabled-fake-harness-evidence-archive-extraction`. |
| J13 | v1803 | completed (Claude executor closeout 2026-06-15) | Seventh contract-preserving ops extraction 闁?second dependency-injected "evidence" registry family and the FIRST to move a whole family owning two controllers + two registry sub-clusters that share one route-path class (sandbox-connection blocked-execution-context dossier + precheck upstream-receipt verification manifest). Moves twenty-six implementation files (25 impl + the shared route-path class) into `ops.maintenance.sandboxconnection`, lowering direct root `ops` Java files from 1,269 to 1,243 (largest single reduction so far), keeping both root controllers + shared route aggregation stable. Family `OpsShardReadinessSandboxConnectionRoutePaths` made public (public BASE_PATH + 2 suffixes); both controllers + aggregator import it. Both services are DI-built with the shared `OpsEvidenceService` and tests build them via `OpsEvidenceServiceTestFixtures`, so three behaviour-neutral visibility steps were needed: moved services/catalogs/support import the public `OpsEvidenceService`/`ReleaseApprovalRehearsalResponse`; the single shared `闁炽儺姣燗NDBOX_CONNECTION_PRECHECK_PACKET_ECHO_RECEIPT_SCHEMA_VERSION` constant on `OpsEvidenceService` was made public; the moved test supports reuse the already-public `OpsEvidenceServiceTestFixtures`. Package-local catalog/source/boundary/immutability/split tests + both test supports moved to subpackage; both route/controller tests stay in root and construct their service directly via the shared fixture. Relocated the two moved Responses' accepted `EI_EXPOSE_REP/REP2` exclusions (11 entries) to the new FQN; the `ReleaseApproval*SandboxConnection*Records` exclusions stay in root (records do not move). Walkthrough written before verify. Added `docs/ops/sandbox-connection-extraction-v1803.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1803Tests`; lowered the ratchet 1269闁?243. Executor closeout: **full `mvnw verify` BUILD SUCCESS 闁?tests 0 failures, JaCoCo "all coverage checks have been met", SpotBugs check passed (0 new), Spotless clean**. Tagged `v1803-order-platform-production-excellence-ops-sandbox-connection-extraction`. |
| J17 | v1807 | completed (Claude executor closeout 2026-06-15) | Tenth contract-preserving ops extraction 闁?FIRST to apply the cross-family ENDPOINT sub-recipe (v1806 was Codex's doc-only quality closeout = J16, no code move). Moves the operator-evidence-value-draft registry family (16 non-controller files + family route-path class) into the new `ops.maintenance.operatorevidencevaluedraft` subpackage; 2 controllers + aggregator stay root. Direct root `ops` Java files 1,183 闁?1,167 (闁?6). Family chosen as the minimal-cut substantial family (own RoutePaths; verified ~6 out-edges to ImportPreflight services + 1 in-edge file). Cross-family handling (visibility only, no route change): made 7 `OperatorEvidenceImportPreflight` service `ENDPOINT` consts public (read outbound by relocated value-draft files) and the value-draft service `ENDPOINT` consts public (read inbound by root `OperatorEvidenceValueSupplySlotCatalog` + the value-draft route guard test), adding imports across the boundary. Compiler-guided (the direct census undercounted 闁?`ValueSupplySlotCatalog` read 8 value-draft ENDPOINTs, RoutePathsTests read more; publicized all value-draft ENDPOINTs to be safe). Family route-path class made public + public BASE_PATH + public suffix consts; services repointed from the package-private aggregator to the family route-path class. Relocated 2 SpotBugs EI_EXPOSE entries; 2 tests moved, 2 stay root. Walkthrough written before verify. Added `docs/ops/operator-evidence-value-draft-extraction-v1807.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1807Tests`; ratchet 1183闁?167. Executor closeout: **full `mvnw verify` BUILD SUCCESS 闁?tests 0 failures, JaCoCo "all coverage checks have been met", SpotBugs check passed (0 new), Spotless clean**. Tagged `v1807-order-platform-production-excellence-ops-operator-evidence-value-draft-extraction`. |
| J18 | v1808 | completed; remote CI passed | Eleventh contract-preserving ops extraction moves the operator-evidence-import-preflight registry family (15 non-controller files + family route-path class) into `ops.maintenance.operatorevidenceimportpreflight`; 2 controllers + aggregator stay root. Direct root `ops` Java files 1,167 -> 1,152. Continues the cross-family ENDPOINT sub-recipe by publicizing immutable upstream `ManualEvidenceWorksheet` and `RuntimeExecutionLiveReadGate` endpoint constants read by the moved services; no route string, response shape, write boundary, credential boundary, deployment, rollback, or archive layout changes. Added `docs/ops/operator-evidence-import-preflight-extraction-v1808.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1808Tests`; ratchet 1167->1152. Local focused tests, Spotless, full `mvnw verify` (1523 tests, JaCoCo, SpotBugs), and GitHub Actions run `27660455459` passed. |
| J19 | v1809 | local gates passed; remote CI pending | Twelfth contract-preserving ops extraction moves the manual-evidence-worksheet registry family (15 non-controller files + family route-path class) into `ops.maintenance.manualevidenceworksheet`; 2 controllers + aggregator stay root. Direct root `ops` Java files 1,152 -> 1,137. Continues the cross-family ENDPOINT sub-recipe upstream of `OperatorEvidenceImportPreflight`: downstream import-preflight services import worksheet endpoints from the new package, and worksheet services publicize immutable `RuntimeExecution` endpoint constants they already referenced. No route string, response shape, write boundary, credential boundary, deployment, rollback, or archive layout changes. Added `docs/ops/manual-evidence-worksheet-extraction-v1809.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1809Tests`; ratchet 1152->1137. Local focused tests, Spotless, and full `mvnw verify` passed with 1527 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. |
| J20 | v1810 | local gates passed; remote CI pending | Thirteenth contract-preserving ops extraction moves the operator-evidence-value-supply signed-approval capture-artifact-preflight registry family (16 non-controller files) into `ops.maintenance.signedapprovalcaptureartifactpreflight`; 2 controllers + aggregator stay root. Direct root `ops` Java files 1,137 -> 1,121. The moved services repoint from the package-private root aggregator to the public family route owner `OpsShardReadinessSignedApprovalCaptureArtifactPreflightRoutePaths` (already in `ops.maintenance.signedapproval` from v1804); v1810 only adds its public `BASE_PATH`. Cross-family ENDPOINT sub-recipe: the family `FragmentCatalog` reads ten sibling `CapturePreflight` endpoint constants (now publicized immutable read-only strings + imported), and the retained-root `ArtifactDraftReadiness` item catalogs read this family's endpoint constants (likewise publicized). No route string, response shape, write boundary, credential boundary, deployment, rollback, or archive layout changes. Added `docs/ops/signed-approval-capture-artifact-preflight-extraction-v1810.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1810Tests`; ratchets `MAX_ROOT_OPS_MAIN_JAVA_FILES` and `EXPECTED_ROOT_OPS_MAIN_JAVA_FILES` 1137->1121. Local focused tests, Spotless, and full `mvnw verify` passed with 1531 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. |
| J21 | v1811 | local gates passed; remote CI pending | Fourteenth contract-preserving ops extraction moves the operator-evidence-value-supply signed-approval capture-preflight registry family (16 non-controller files) into `ops.maintenance.signedapprovalcapturepreflight`; 2 controllers + aggregator stay root. Direct root `ops` Java files 1,121 -> 1,105. The moved services repoint from the package-private root aggregator to the public family route owner `OpsShardReadinessSignedApprovalCapturePreflightRoutePaths` (already in `ops.maintenance.signedapproval` from v1804); v1811 only adds its public `BASE_PATH`. Cross-family ENDPOINT sub-recipe: the moved `InputCatalog` reads eleven upstream `ApprovalPreflight` endpoint constants (now publicized immutable read-only strings + imported), and the already-moved v1810 `CaptureArtifactPreflightFragmentCatalog` reads ten sibling `CapturePreflight` endpoint constants from the new package. No route string, response shape, write boundary, credential boundary, deployment, rollback, or archive layout changes. Added `docs/ops/signed-approval-capture-preflight-extraction-v1811.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1811Tests`; ratchets `MAX_ROOT_OPS_MAIN_JAVA_FILES`, `EXPECTED_ROOT_OPS_MAIN_JAVA_FILES`, and the exact measured root guard 1121->1105. Local focused walkthrough compliance test passed, and full `mvnw verify` passed with 1535 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. |
| J22 | v1812 | local gates passed; remote CI pending | Fifteenth contract-preserving ops extraction moves the operator-evidence-value-supply approval-preflight registry family into `ops.maintenance.approvalpreflight`: 15 physical implementation files move, the package-private policy catalog is collocated with the item catalog to offset the new route owner, and 2 controllers + aggregator stay root. Direct root `ops` Java files 1,105 -> 1,089 while total `ops` Java files stay at 1,352. The new public `OpsShardReadinessOperatorEvidenceValueSupplyApprovalPreflightRoutePaths` owner holds the family suffix constants while the root aggregator delegates to it, preserving route strings. Cross-family ENDPOINT sub-recipe: the moved `ItemCatalog` reads seven upstream value-supply or adapter-preflight endpoint constants that are now public immutable strings, and the v1811 `SignedApprovalCapturePreflightInputCatalog` imports approval-preflight endpoint constants from the new package. No route string, response shape, write boundary, credential boundary, deployment, rollback, or archive layout changes. Added `docs/ops/approval-preflight-extraction-v1812.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1812Tests`; ratchets `MAX_ROOT_OPS_MAIN_JAVA_FILES`, `EXPECTED_ROOT_OPS_MAIN_JAVA_FILES`, and the exact measured root guard 1105->1089. Local focused tests passed with 42 tests, Spotless passed, and full `mvnw verify` passed with 1541 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. |
| J23 | v1813 | local gates passed; remote CI pending | Sixteenth contract-preserving ops extraction moves the operator-evidence-value-supply signed-approval artifact-draft-readiness registry family (16 non-controller files) into `ops.maintenance.signedapprovalartifactdraftreadiness`; 2 controllers + aggregator stay root. Direct root `ops` Java files 1,089 -> 1,073. Moved services repoint from the package-private root aggregator to the public family route owner `OpsShardReadinessSignedApprovalArtifactDraftReadinessRoutePaths` (already in `ops.maintenance.signedapproval` from v1804); v1813 only adds its public `BASE_PATH`. Cross-family ENDPOINT sub-recipe: the moved item catalogs read ten `CaptureArtifactPreflight` endpoint constants already public from v1810 (no new outbound change), and this family's own endpoint constants are publicized for three retained-root sibling readers (`ArtifactDraftPreflight`, `ArtifactDraftReviewPackagePreflight`, `SignedApprovalDraftProfileSection`). Relocated the two `ArtifactDraftReadinessResponse` EI_EXPOSE spotbugs FQNs. No route string, response shape, write boundary, credential boundary, deployment, rollback, or archive layout changes. Added `docs/ops/signed-approval-artifact-draft-readiness-extraction-v1813.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1813Tests`; ratchets `MAX_ROOT_OPS_MAIN_JAVA_FILES`, `EXPECTED_ROOT_OPS_MAIN_JAVA_FILES`, and the exact measured root guard 1089->1073. Local focused tests, Spotless, and full `mvnw verify` passed with 1545 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. |
| J24 | v1814 | local gates passed; remote CI pending | Seventeenth contract-preserving ops extraction moves the operator-evidence-value-supply signed-approval artifact-draft-preflight registry family into `ops.maintenance.signedapprovalartifactdraftpreflight`: 15 physical implementation files move, the package-private gate catalog is collocated with the guard catalog to offset the new route owner, and 2 controllers + aggregator stay root. Direct root `ops` Java files 1,073 -> 1,057 while total `ops` Java files stay at 1,352. The new public `OpsShardReadinessSignedApprovalArtifactDraftPreflightRoutePaths` owner holds the family suffix constants while the root aggregator delegates to it, preserving route strings. Cross-family ENDPOINT sub-recipe: the moved field catalogs read already-public v1813 `ArtifactDraftReadiness` endpoint constants, and retained-root `ArtifactDraftReadinessLane` + `SignedApprovalDraftProfileSection` readers import this family's public immutable endpoint strings. No route string, response shape, write boundary, credential boundary, deployment, rollback, or archive layout changes. Added `docs/ops/signed-approval-artifact-draft-preflight-extraction-v1814.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1814Tests`; ratchets `MAX_ROOT_OPS_MAIN_JAVA_FILES`, `EXPECTED_ROOT_OPS_MAIN_JAVA_FILES`, and the exact measured root guard 1073->1057. Local focused tests passed with 78 tests, and full `mvnw verify` passed with 1551 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. |
| J25 | v1815 | local gates passed; remote CI pending | Eighteenth contract-preserving ops extraction moves the operator-evidence-value-supply signed-approval artifact-draft-readiness-lane registry family into `ops.maintenance.signedapprovalartifactdraftreadinesslane`: 15 physical implementation files move, the package-private gate catalog is collocated with the blocker catalog to offset the new route owner, and 2 controllers + aggregator stay root. Direct root `ops` Java files 1,057 -> 1,041 while total `ops` Java files stay at 1,352. The new public `OpsShardReadinessSignedApprovalArtifactDraftReadinessLaneRoutePaths` owner holds the family suffix constants while the root aggregator delegates to it, preserving route strings. Cross-family ENDPOINT sub-recipe: the moved lane catalogs read already-public v1814 `ArtifactDraftPreflight` endpoint constants, and retained-root `ReviewPackagePreflight` slot catalogs import this family's public immutable endpoint strings. No route string, response shape, write boundary, credential boundary, deployment, rollback, or archive layout changes. Added `docs/ops/signed-approval-artifact-draft-readiness-lane-extraction-v1815.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1815Tests`; ratchets `MAX_ROOT_OPS_MAIN_JAVA_FILES`, `EXPECTED_ROOT_OPS_MAIN_JAVA_FILES`, and the exact measured root guard 1057->1041. Local focused tests passed with 43 tests, Spotless passed, and full `mvnw verify` passed with 1557 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. |
| J26 | v1816 | local gates passed; remote CI pending | Nineteenth contract-preserving ops extraction moves the operator-evidence-value-supply signed-approval artifact-draft-review-package-preflight registry family into `ops.maintenance.signedapprovalartifactdraftreviewpackagepreflight`: 15 physical implementation files move, the package-private gate catalog is collocated with the guard catalog to offset the new route owner, and 2 controllers + aggregator stay root. Direct root `ops` Java files 1,041 -> 1,025 while total `ops` Java files stay at 1,352. The new public `OpsShardReadinessSignedApprovalArtifactDraftReviewPackagePreflightRoutePaths` owner holds the family suffix constants while the root aggregator delegates to it, preserving route strings. Cross-family ENDPOINT sub-recipe: the moved slot catalogs read already-public v1815 `ArtifactDraftReadinessLane` endpoint constants, and retained-root `AuthoringReadiness` plus `SignedApprovalDraftProfileSection` readers import this family's public immutable endpoint strings. No route string, response shape, write boundary, credential boundary, deployment, rollback, or archive layout changes. Added `docs/ops/signed-approval-artifact-draft-review-package-preflight-extraction-v1816.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1816Tests`; ratchets `MAX_ROOT_OPS_MAIN_JAVA_FILES`, `EXPECTED_ROOT_OPS_MAIN_JAVA_FILES`, and the exact measured root guard 1041->1025. Local focused tests passed with 100 tests, Spotless passed, and full `mvnw verify` passed with 1563 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. |
| J27 | v1817 | local gates passed; remote CI pending | Twentieth contract-preserving ops extraction moves the operator-evidence-value-supply signed-approval artifact-draft-authoring-readiness registry family into `ops.maintenance.signedapprovalartifactdraftauthoringreadiness`: 15 physical implementation files move, the package-private gate catalog is collocated with the blocker catalog to offset the new route owner, and 2 controllers + aggregator stay root. Direct root `ops` Java files 1,025 -> 1,009 while total `ops` Java files stay at 1,352. The new public `OpsShardReadinessSignedApprovalArtifactDraftAuthoringReadinessRoutePaths` owner holds the family suffix constants while the root aggregator delegates to it, preserving route strings. Cross-family ENDPOINT sub-recipe: the moved requirement catalogs read already-public v1816 `ArtifactDraftReviewPackagePreflight` endpoint constants, and retained-root `InstructionPreflight` plus `SignedApprovalDraftProfileSection` readers import this family's public immutable endpoint strings. No route string, response shape, write boundary, credential boundary, deployment, rollback, or archive layout changes. Added `docs/ops/signed-approval-artifact-draft-authoring-readiness-extraction-v1817.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1817Tests`; ratchets `MAX_ROOT_OPS_MAIN_JAVA_FILES`, `EXPECTED_ROOT_OPS_MAIN_JAVA_FILES`, and the exact measured root guard 1025->1009. Local focused tests passed with 106 tests, Spotless passed, and full `mvnw verify` passed with 1568 tests, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. |
| J28 | v1818 | local gates passed; remote CI pending | Twenty-first contract-preserving ops extraction moves the operator-evidence-value-supply signed-approval artifact-draft-instruction-preflight registry family into `ops.maintenance.signedapprovalartifactdraftinstructionpreflight`: 15 physical implementation files move, the package-private gate catalog is collocated with the guard catalog to offset the new route owner, and 2 controllers + aggregator stay root. Direct root `ops` Java files 1,009 -> 993 while total `ops` Java files stay at 1,352. The new public `OpsShardReadinessSignedApprovalArtifactDraftInstructionPreflightRoutePaths` owner holds the family suffix constants while the root aggregator delegates to it, preserving route strings. Cross-family ENDPOINT sub-recipe: the moved slot catalogs read already-public v1817 `ArtifactDraftAuthoringReadiness` endpoint constants, and retained-root `TextPackageIntake` plus `SignedApprovalDraftProfileSection` readers import this family's public immutable endpoint strings. No route string, response shape, write boundary, credential boundary, deployment, rollback, or archive layout changes. Added `docs/ops/signed-approval-artifact-draft-instruction-preflight-extraction-v1818.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1818Tests`; ratchets `MAX_ROOT_OPS_MAIN_JAVA_FILES`, `EXPECTED_ROOT_OPS_MAIN_JAVA_FILES`, and the exact measured root guard 1009->993. Local focused tests, Spotless, and full `mvnw verify` passed (1,574 tests, JaCoCo coverage checks met, SpotBugs 0 findings). |
| J15 | v1805 | completed (Claude executor closeout 2026-06-15) | Ninth contract-preserving ops extraction 闁?LARGEST so far. Moves the entire candidate-document registry family (57 non-controller files + family route-path class) into the new `ops.maintenance.candidatedocument` subpackage; eight public controllers + the aggregator stay in root. Direct root `ops` Java files 1,240 闁?1,183 (闁?7). Picked via a parallel edge-census workflow that ranked candidates by cross-family ENDPOINT edge count: CandidateDocument scored zero ENDPOINT edges (lowest), then adversarial direct verification confirmed it (correcting a stale "coupled" memory note). Intra-family DI (registry services compose sibling candidate-document services) so the family moved as one unit with no cross-package wiring. Family route-path class made public + public BASE_PATH + public suffix consts; relocated services repointed from the package-private aggregator to the family route-path class. ONE genuine cross-family edge handled: two candidate-document catalogs read the compared-evidence candidate-intake-preflight catalog route (aggregator-inline) 闁?moved that constant into the candidate-document route-path class as owner + aggregator now delegates to it (compared-evidence family keeps the same value via the aggregator). Relocated 19 SpotBugs EI_EXPOSE entries across 9 response classes; made 2 shared test-support classes public for retained root tests (11 tests stay root, 30 moved). Walkthrough written before verify. Added `docs/ops/candidate-document-extraction-v1805.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1805Tests`; ratchet 1240闁?183. Executor closeout: **full `mvnw verify` BUILD SUCCESS 闁?tests 0 failures, JaCoCo "all coverage checks have been met", SpotBugs check passed (0 new), Spotless clean**. Tagged `v1805-order-platform-production-excellence-ops-candidate-document-extraction`. |
| J16 | v1806 | local gates passed; remote CI pending | Java extraction quality closeout records the historical v1798 tag exception, the v1799 remediation boundary, the v1805 current green baseline, and the policy not to rewrite or force-move historical tags. Do not rewrite or force-move historical tags. Adds `docs/ops/java-extraction-quality-closeout-v1806.md` and `ReadabilityUpkeepOpsConsolidationQualityCloseoutV1806Tests`; no business code, route string, response schema, runtime profile, deployment, rollback, or archive layout changes. Local focused closeout tests passed (`14 tests, 0 failures`), Spotless passed, and full `mvnw verify` passed in 8m16s with `1516 tests, 0 failures`, JaCoCo coverage checks met, and SpotBugs reporting 0 findings. |
| J14 | v1804 | completed (Claude executor closeout 2026-06-15) | Eighth contract-preserving ops extraction 闁?FIRST pure route-path leaf consolidation (no service/controller/response moves). Moves three signed-approval route-path classes (`OpsShardReadinessSignedApproval{ArtifactDraftReadiness,CaptureArtifactPreflight,CapturePreflight}RoutePaths`) into the new `ops.maintenance.signedapproval` subpackage, lowering direct root `ops` Java files from 1,243 to 1,240; stands up the signedapproval subpackage for later migration of the operator-evidence-value-supply signed-approval registry families. Made the three classes + their `OPERATOR_EVIDENCE_VALUE_SUPPLY_SIGNED_APPROVAL_*` suffix constants public (behaviour-neutral). Each class is referenced directly only by the root aggregator (still delegating the public suffix) + its single `...RoutePathsTests` guard; both repointed by import only 闁?the owning registry services/controllers stay in root and read the suffixes via the aggregator, so endpoint strings are byte-identical. Smaller by design: the remaining substantial families (ManualEvidenceWorksheet, OperatorEvidence*, the signed-approval registries) are aggregator-backed AND cross-coupled through each other's ENDPOINT constants, so the clean single-step targets are now the route-path leaves; the bigger families need a cross-family ENDPOINT-publicization sub-recipe. Walkthrough written before verify. Added `docs/ops/signed-approval-route-path-consolidation-v1804.md` + `ReadabilityUpkeepOpsConsolidationExtractionV1804Tests`; lowered the ratchet 1243闁?240. Executor closeout: **full `mvnw verify` BUILD SUCCESS 闁?tests 0 failures, JaCoCo "all coverage checks have been met", SpotBugs check passed (0 new), Spotless clean**. Tagged `v1804-order-platform-production-excellence-ops-signed-approval-route-path-consolidation`. |
