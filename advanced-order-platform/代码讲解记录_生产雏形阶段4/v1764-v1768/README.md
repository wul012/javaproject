# Java v1764-v1768 f archive correction batch

This directory contains the code walkthroughs for the canonical `f/` screenshot/explanation archive correction.

Runtime screenshot explanation artifacts for this batch are represented by:

```text
f/v1764-v1768/
```

## Range

- v1764: registry root correction from transitional `d_runtime_screenshot_archive_next` to `f`.
- v1765: `f/` root documentation and historical-root policy update.
- v1766: `f/` root guard tests and no-root-dumping checks.
- v1767: code walkthrough and archive index updates.
- v1768: closeout verification and tag handoff.

## Boundary

This batch defines archive policy only. It does not capture screenshots, move historical archives, enable write routing, read credentials, resolve raw endpoints, contact managed audit, deploy, rollback, autostart Java, or autostart mini-kv.
