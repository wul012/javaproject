# 第五十版：ops evidence 只读窗口自描述增强

## 计划依据

本版推进前只读查看最新计划：

```text
D:\nodeproj\orderops-node\docs\plans\v153-post-operator-runbook-roadmap.md
```

计划里对 Java v50 的目标是：

```text
Java v50 + mini-kv v59：下一步推进，可以一起推进，只补真实只读联调窗口需要的“启动后自描述/健康证据”增强。
Java 补 ops health/evidence 字段稳定性。
```

## 自检结论

当前计划合理：

```text
Java 最新 tag 是 v49订单平台ops-read-only-evidence-sample。
Node v154-v155 已完成，Node v156 等待 Java / mini-kv 启动后的只读证据。
本版只增强 Java 动态 evidence 和静态样本字段，不新增写操作。
本版不修改 Node，不修改 mini-kv。
```

## 本版目标

增强动态只读 evidence：

```text
GET /api/v1/ops/evidence
```

新增稳定字段：

```text
healthProbe
readOnlyWindow
```

核心语义：

```text
healthProbe.endpoint=/actuator/health
healthProbe.expectedStatus=UP
readOnlyWindow.windowVersion=java-read-only-window.v1
readOnlyWindow.operatorStartRequired=true
readOnlyWindow.nodeAutoStartAllowed=false
readOnlyWindow.upstreamProbesRequired=true
readOnlyWindow.upstreamActionsAllowed=false
readOnlyWindow.readyForReadOnlyLiveProbe=true
readOnlyWindow.readyForProductionOperations=false
```

静态样本也补同样字段，但固定：

```text
healthProbe.staticSampleOnly=true
readOnlyWindow.readyForReadOnlyLiveProbe=false
```

## 代码改动

更新响应模型：

```text
src/main/java/com/codexdemo/orderplatform/ops/OpsEvidenceResponse.java
```

更新动态 evidence 组装：

```text
src/main/java/com/codexdemo/orderplatform/ops/OpsEvidenceService.java
```

更新静态样本：

```text
src/main/resources/static/contracts/ops-read-only-evidence.sample.json
```

更新测试：

```text
src/test/java/com/codexdemo/orderplatform/OpsOverviewIntegrationTests.java
src/test/java/com/codexdemo/orderplatform/ops/OpsEvidenceServiceTests.java
```

更新文档：

```text
README.md
代码讲解记录_生产雏形阶段/README.md
代码讲解记录_生产雏形阶段/54-version-50-ops-read-only-window-self-description.md
b/50/解释/说明.md
```

## 验证记录

聚焦测试：

```text
mvn "-Dtest=OpsOverviewIntegrationTests,OpsEvidenceServiceTests" test
```

结果：

```text
Tests run: 4, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

非 Docker 测试：

```text
mvn "-Dtest=!PostgresMigrationIntegrationTests,!RabbitMqNotificationConsumerIntegrationTests,!RabbitMqNotificationFailureIntegrationTests,!RabbitMqOutboxPublisherIntegrationTests" "-DargLine=-XX:TieredStopAtLevel=1 -Xmx512m" test
```

结果：

```text
Tests run: 64, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

Docker / Testcontainers 集成测试：

```text
mvn "-Dtest=PostgresMigrationIntegrationTests,RabbitMqNotificationConsumerIntegrationTests,RabbitMqNotificationFailureIntegrationTests,RabbitMqOutboxPublisherIntegrationTests" test
```

结果：

```text
Tests run: 4, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

打包：

```text
mvn "-DskipTests" package
BUILD SUCCESS
```

HTTP smoke：

```text
GET /actuator/health
 -> health=UP

GET /api/v1/ops/evidence
 -> evidenceVersion=java-ops-evidence.v1
 -> healthProbe.endpoint=/actuator/health
 -> healthProbe.expectedStatus=UP
 -> healthProbe.staticSampleOnly=false
 -> readOnlyWindow.windowVersion=java-read-only-window.v1
 -> readOnlyWindow.readyForReadOnlyLiveProbe=true
 -> readOnlyWindow.readyForProductionOperations=false
 -> readOnlyWindow.upstreamActionsAllowed=false
 -> forbiddenOperations 包含 POST /api/v1/failed-events/{id}/replay
 -> requiredNodeEnvironment 包含 UPSTREAM_ACTIONS_ENABLED=false

GET /contracts/ops-read-only-evidence.sample.json
 -> healthProbe.staticSampleOnly=true
 -> readOnlyWindow.readyForReadOnlyLiveProbe=false
 -> forbiddenOperations 包含 Any non-GET Node upstream action
```

图片记录：

```text
b/50/图片/01-focused-test.png
b/50/图片/02-non-docker-test.png
b/50/图片/03-docker-integration-test.png
b/50/图片/04-package.png
b/50/图片/05-http-smoke.png
b/50/图片/06-cleanup.png
```

清理记录：

```text
HTTP smoke 启动的 Java 进程已停止
18092 端口已释放
Testcontainers 容器已退出
target 已通过 mvn clean 删除
tmp-v50-logs 已删除
Docker 已按 AGENTS.md 快速关闭路径关闭
```

## 边界

本版不做：

```text
不执行订单写操作
不执行 Java replay POST
不发布 RabbitMQ replay 消息
不修改数据库结构
不修改 Node
不修改 mini-kv
不把静态样本伪装成 live upstream pass
```

## 一句话总结

v50 让 Java 启动后的 ops evidence 能自描述真实只读窗口需要的健康检查、允许 GET、禁止写操作和 Node 环境开关，为 Node v156 只读 live probe capture 提供稳定 Java 输入。
