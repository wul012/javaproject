# Java v114 运行说明：verification hint catalog split

## 计划依据

本次继续读取当前有效计划：

```text
D:\nodeproj\orderops-node\docs\plans\v272-post-plan-intake-echo-roadmap.md
```

计划里 Java v113 的业务回显已经完成；后续质量优化项明确保留：

```text
ReleaseApprovalVerificationHintBuilder.java 约 860 行，作为中优先级拆分项保留。
不写 approval ledger。
不执行 SQL。
不读取 credential value。
不打开 managed audit connection。
```

所以 v114 做的是 contract-preserving refactor，不新增业务 receipt，不改变 rehearsal response schema。

## 合理性判断

合理。v113 后 `ReleaseApprovalVerificationHintBuilder` 已经承接太多固定清单：

```text
schemaFields
warningDigestInputs 的固定 warning/boundary 字段
proofClaims 的固定 no-write 声明
nodeVerificationActions 的固定核验动作
内部 contribution record
```

这些内容大多是稳定目录，不是 builder 的核心编排职责。把固定清单抽到 catalog，可以让 builder 继续专注于：

```text
调用 warning digest builder
拼接动态 verification contributions
执行 noLedgerWriteProved 判断
返回 RehearsalVerificationHint
```

## 代码改动

新增：

```text
src/main/java/com/codexdemo/orderplatform/ops/ReleaseApprovalVerificationHintCatalog.java
src/main/java/com/codexdemo/orderplatform/ops/ReleaseApprovalVerificationHintContribution.java
```

修改：

```text
src/main/java/com/codexdemo/orderplatform/ops/ReleaseApprovalVerificationHintBuilder.java
```

`ReleaseApprovalVerificationHintCatalog` 负责保存固定清单：

```text
warningDigestWarningInputNames
warningDigestBoundaryInputNames
finalWarningDigestBoundaryInputName
proofClaims
closingProofClaims
schemaFields
nodeVerificationActions
closingNodeVerificationActions
```

`ReleaseApprovalVerificationHintContribution` 负责承接动态 supplier：

```text
warningDigestWarningInputNames
warningDigestBoundaryInputNames
proofClaims
nodeVerificationActions
```

`ReleaseApprovalVerificationHintBuilder` 改为组合这些清单和动态 contribution，保持输出顺序不变。

## 行数结果

```text
ReleaseApprovalVerificationHintBuilder.java: 903 -> 648 行
ReleaseApprovalVerificationHintCatalog.java: 269 行
ReleaseApprovalVerificationHintContribution.java: 27 行
OpsEvidenceService.java: 997 行，未增长
```

这次拆分幅度适中：主 builder 少了 255 行，抽出的 catalog 是纯静态目录，没有把 no-write 业务判断拆散。

## 边界

本版不改变：

```text
response schema version
verification hint version
warningDigest 输入顺序
proofClaims 顺序
nodeVerificationActions 顺序
noLedgerWriteProved 判定
credential value 访问边界
raw endpoint 解析边界
managed audit connection 边界
SQL / ledger / schema migration / auto-start 边界
```

本版没有启动 Docker，也没有必要启动 Docker。变更是 Java 内部只读结构拆分，Maven 编译和现有 Spring/H2 集成测试足以覆盖。

## 验证记录

已运行：

```text
mvn -q -DskipTests compile
mvn -q "-Dtest=OpsEvidenceServiceTests,OpsOverviewIntegrationTests" test
```

覆盖点：

```text
ReleaseApprovalVerificationHintBuilder 编译通过
OpsEvidenceServiceTests 保持通过
OpsOverviewIntegrationTests 保持通过
HTTP JSON 字段仍可见
verification hint 清单顺序未破坏
read-only / no-ledger / no-credential / no-connection 边界保持
```

## 清理记录

测试会生成 Maven `target/`；最终交付前删除。没有启动长驻 Java 服务，没有启动 Docker Desktop，没有遗留后台进程。

## 一句话总结

Java v114 把 `ReleaseApprovalVerificationHintBuilder` 的固定清单和 contribution record 拆成独立 helper，让主 builder 从 903 行降到 648 行，同时保持响应契约、digest 顺序、proof claims 和 managed-audit 安全边界全部不变。
